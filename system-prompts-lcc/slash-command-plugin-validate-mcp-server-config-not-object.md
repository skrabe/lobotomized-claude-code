<!--
name: 'Slash Command: /plugin validate MCP server config not an object'
description: >-
  Plugin validator error: an MCP server entry is not an object, so the plugin
  loader silently drops it at load.
ccVersion: 2.1.281
variables:
  - SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_CONFIG_NOT_OBJECT_VAR_0
  - SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_CONFIG_NOT_OBJECT_VAR_1
-->
server config must be an object (got ${SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_CONFIG_NOT_OBJECT_VAR_0(SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_CONFIG_NOT_OBJECT_VAR_1)}). The plugin loader silently drops this server at load.
