<!--
name: Artifact Files Service View Name
description: >-
  Tool result when a published files path is a name the artifact service answers
  with its own view.
ccVersion: 2.1.261
variables:
  - TOOL_RESULT_ARTIFACT_FILES_SERVICE_VIEW_NAME_VAR_0
  - TOOL_RESULT_ARTIFACT_FILES_SERVICE_VIEW_NAME_VAR_1
-->
files: published path ${TOOL_RESULT_ARTIFACT_FILES_SERVICE_VIEW_NAME_VAR_0(TOOL_RESULT_ARTIFACT_FILES_SERVICE_VIEW_NAME_VAR_1)} is a name the artifact service answers with one of its own views, so the file could never be read back — rename it
