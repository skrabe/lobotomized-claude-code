<!--
name: 'Tool Result: Isolation worktree git dir not comparable'
description: >-
  Reason fragment inserted into the "isolation refused" EnterWorktree error when
  the worktree's git directory cannot be safely compared with the protected
  checkout's.
ccVersion: 2.1.224
variables:
  - TOOL_RESULT_WORKTREE_ISOLATION_UNVERIFIABLE_GIT_DIR_VAR_0
-->
its git directory (${TOOL_RESULT_WORKTREE_ISOLATION_UNVERIFIABLE_GIT_DIR_VAR_0.gitDir}) could not be safely compared with the protected checkout's
