<!--
name: 'Data: sandbox.credentials.awsPairs setting description (part 3 of 5)'
description: >-
  Description of the `sandbox.credentials.awsPairs` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
.claude/settings.local.json) are ignored. A member is only usable when its env var is forwarded as a whole-value `mask` entry (an entry carrying `extract` or `decode` does not 
