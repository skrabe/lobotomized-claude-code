<!--
name: NL date/time parser — INVALID rule
description: >-
  System-prompt line instructing the parser to respond 'INVALID' when the input
  can't be confidently parsed.
ccVersion: 2.1.206
-->
If the input is incomplete or you cannot confidently parse it into a valid date, respond with exactly "INVALID" (nothing else).
