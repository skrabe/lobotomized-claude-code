<!--
name: 'Tool Result: Isolation worktree is a symlink'
description: >-
  Reason fragment inserted into the "isolation refused" EnterWorktree error when
  the candidate worktree path is itself a symbolic link.
ccVersion: 2.1.224
-->
it is a symbolic link, so the tree it would isolate into cannot be pinned down
