<!--
name: PR Review MCP Server Entry Unknown Fields
description: >-
  k9o() manifest-validation failure telling Claude the mcp server entry carries
  fields other than `server` and `tools`.
ccVersion: 2.1.221
-->

the review page mcp server entry carries unknown fields — only `server` and `tools` are allowed
