<!--
name: 'Data: sandbox.credentials.envVars.mode setting description (part 1 of 2)'
description: >-
  Description of the `sandbox.credentials.envVars.mode` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Access mode for this environment variable. `deny` unsets the variable for sandboxed commands; `mask` shows sandboxed commands a sentinel value and the 
