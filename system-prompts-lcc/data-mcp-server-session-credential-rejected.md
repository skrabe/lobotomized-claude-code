<!--
name: MCP server credential-rejected error
description: >-
  Failed-server .error text (HTTP 401 session credential rejected) quoted into
  the model-facing "MCP servers failed to connect" system-reminder via DZn.
ccVersion: 2.1.210
variables:
  - DATA_MCP_SERVER_SESSION_CREDENTIAL_REJECTED_VAR_0
-->
Server rejected the session credential (HTTP ${DATA_MCP_SERVER_SESSION_CREDENTIAL_REJECTED_VAR_0??401}). It will be retried when the session credential is refreshed.
