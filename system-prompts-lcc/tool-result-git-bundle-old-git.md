<!--
name: 'Tool Result: Git Bundle Git Older Than 2.31'
description: >-
  Refuses the upload when the installed git is older than 2.31 so
  configuration-named programs cannot be kept from running while the bundle is
  prepared.
ccVersion: 2.1.246
-->
Not uploading this working tree: the installed git is older than 2.31, which cannot be kept from running programs this checkout’s configuration names while the upload is prepared. Update git (2.31 or newer), then retry.
