<!--
name: 'Tool Result: Task output truncated (session writes no files)'
description: >-
  Suffix appended to background task output when it was truncated because the
  session writes no files, so later output was discarded.
ccVersion: 2.1.281
variables:
  - TOOL_RESULT_TASK_OUTPUT_TRUNCATED_NO_FILES_SESSION_VAR_0
-->

Output truncated (${TOOL_RESULT_TASK_OUTPUT_TRUNCATED_NO_FILES_SESSION_VAR_0.round(this.#l/1024)}KB total): later output was discarded because this session writes no files.
