<!--
name: 'System Reminder: Dir Sync Host Abandoned Push'
description: >-
  dir_sync_notice that the host gave up sending post-command file changes, so
  they arrive with the user's next message.
ccVersion: 2.1.251
variables:
  - SYSTEM_REMINDER_DIR_SYNC_HOST_ABANDONED_PUSH_VAR_0
-->
Directory sync: ${SYSTEM_REMINDER_DIR_SYNC_HOST_ABANDONED_PUSH_VAR_0.name} gave up sending what the last command run there changed; files it changed there reach this session with the user's next message.
