<!--
name: 'Slash Command: /web-setup — GitHub CLI not installed'
description: >-
  Tells the model setup stopped because the `gh` binary is missing and gives
  both recovery paths (install and authenticate, or connect GitHub in the
  browser).
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_WEB_SETUP_GH_CLI_NOT_INSTALLED_VAR_0
-->
GitHub CLI not found. Install it via https://cli.github.com/, then run \`gh auth login\`, or connect GitHub on the web: ${SLASH_COMMAND_WEB_SETUP_GH_CLI_NOT_INSTALLED_VAR_0}
