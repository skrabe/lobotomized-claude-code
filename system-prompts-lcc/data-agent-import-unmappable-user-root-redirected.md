<!--
name: 'Data: Agent Import — User-Scope Root Redirected'
description: >-
  Reason text emitted when the user-scope read/write root is redirected; pushed
  as a user-scope unmappable and injected into the `/import` prompt and the
  fallback SKILL.md.
ccVersion: 2.1.214
-->
The user-scope read or write root has been redirected (resolves inside this project, to a network path, or away from the real home directory) — skipping user-scope scan for safety.
