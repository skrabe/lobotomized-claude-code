<!--
name: Grep -n line numbers parameter
description: >-
  The `-n` input-schema param description for the Grep tool, serialized into the
  model's tool list.
ccVersion: 2.1.191
-->
Show line numbers in output (rg -n). Requires output_mode: "content", ignored otherwise. Defaults to true.
