<!--
name: 'Slash Command: /advisor — client cannot change the advisor'
description: >-
  Tells the model the advisor setting was left untouched because this client has
  no control channel, so it should not assume the requested advisor is now
  active.
ccVersion: 2.1.233
-->
The advisor can't be changed from this client — this connection is view-only or has no control channel
