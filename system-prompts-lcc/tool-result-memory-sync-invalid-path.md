<!--
name: Memory Sync Invalid Path Rejection
description: >-
  Rejection-reason text (rwo.invalid_path) composed into the PostToolUse
  additionalContext the model reads when a memory file's path is too
  long/deep/invalid; model-facing.
ccVersion: 2.1.201
-->
The server rejected a memory file's path (too long, too deep, or containing invalid characters). Rename the offending file.
