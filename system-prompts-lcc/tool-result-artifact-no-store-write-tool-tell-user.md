<!--
name: 'Tool Result: Artifact — no store-write tool, tell the user'
description: >-
  Branch of the declared-store write guidance for a session that serves no
  store-write call at all: it instructs the model to say so to the user rather
  than attempt a write.
ccVersion: 2.1.273
-->
this session offers no tool that writes an Artifact's store — tell the user that
