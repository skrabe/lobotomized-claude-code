<!--
name: 'Data: Dir Sync — .gitignore Too Many Rules With Those Above'
description: >-
  I_t() reason for a .gitignore that, together with the ones above it, holds
  more rules than sync applies to one path; part of directory-sync refusals.
ccVersion: 2.1.282
variables:
  - DATA_DIR_SYNC_GITIGNORE_TOO_MANY_RULES_ABOVE_VAR_0
-->
holds, together with the .gitignore files above it, more rules than sync applies to one path (${DATA_DIR_SYNC_GITIGNORE_TOO_MANY_RULES_ABOVE_VAR_0.toLocaleString("en-US")}; shorten it or one of those above it)
