<!--
name: 'Slash Command: /plugin eval — queries validated, trigger tests pending'
description: >-
  Tells the model the eval files parsed but no trigger test actually ran because
  model evaluation is not wired up yet, so a green-looking run is not evidence
  the skill triggers.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_PLUGIN_EVAL_OUTCOME_PENDING_MODEL_INTEGRATION_VAR_0
-->


${SLASH_COMMAND_PLUGIN_EVAL_OUTCOME_PENDING_MODEL_INTEGRATION_VAR_0.info} Eval queries validated; trigger tests pending model integration
