<!--
name: >-
  System Reminder: MCP Servers Need Auth — Authorize via claude mcp or
  Interactive /mcp
description: >-
  Where-to-authorize clause (${Nt}) in the MCP needs-auth reminder for
  non-interactive sessions: claude.ai connectors via connector settings, other
  servers via `claude mcp` or /mcp in an interactive session.
ccVersion: 2.1.282
-->
for claude.ai connectors, via their claude.ai connector settings; for other servers, via `claude mcp` or /mcp in an interactive session
