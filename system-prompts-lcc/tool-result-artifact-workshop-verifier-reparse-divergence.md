<!--
name: 'Workshop Verifier: Reparse Divergence'
description: >-
  Document-level verifier refusal returned when the page verifies as written but
  its parse/serialize round trip does not.
ccVersion: 2.1.219
-->
The page verifies as written, but the parse→serialize round trip every decision confirm republishes does not — restructure the markup flagged below so the serialized form stays within contract.
