<!--
name: 'Tool Description: Edit old_string Uniqueness'
description: >-
  Edit tool description fragment explaining the edit fails if old_string is not
  unique; use more context or replace_all.
ccVersion: 2.1.178
-->

- The edit fails if `old_string` is not unique. Provide more surrounding context to make it unique, or use `replace_all` to change every instance.
