<!--
name: 'Slash Command: /sandbox — strict sandbox mode'
description: >-
  Tells the model every command must run inside the sandbox unless listed in
  excludedCommands, and names that setting as the only escape hatch.
ccVersion: 2.1.233
-->
✓ Strict sandbox mode - all commands must run in sandbox or be excluded via the `excludedCommands` option
