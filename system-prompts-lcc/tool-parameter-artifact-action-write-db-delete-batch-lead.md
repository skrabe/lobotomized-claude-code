<!--
name: 'Tool Parameter: Artifact Action write_db Delete/Batch Lead'
description: >-
  Action-enum lead for write_db delete and batch, now a separate literal after
  the optional str_replace clause, before the batch-size concat.
ccVersion: 2.1.265
-->
 'delete' with `collection` + `doc_id`; 'batch' with `writes` (up to 
