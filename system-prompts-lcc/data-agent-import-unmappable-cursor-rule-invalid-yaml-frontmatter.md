<!--
name: 'Data: Agent Import — Cursor Rule Invalid YAML Frontmatter'
description: >-
  Unmappable reason for a Cursor rule whose frontmatter is not valid YAML,
  interpolated into the /import query prompt.
ccVersion: 2.1.265
-->
Its frontmatter is not valid YAML, so its activation mode is unknown. Fix the frontmatter and re-run, or port it manually.
