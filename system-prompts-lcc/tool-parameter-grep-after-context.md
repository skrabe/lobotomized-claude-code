<!--
name: Grep -A After Context Parameter
description: >-
  Model-facing input_schema parameter description for the Grep tool's -A (lines
  after match) field, serialized into the model's tool list.
ccVersion: 2.1.191
-->
Number of lines to show after each match (rg -A). Requires output_mode: "content", ignored otherwise.
