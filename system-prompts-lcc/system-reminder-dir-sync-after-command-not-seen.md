<!--
name: Dir Sync After Command Not Seen
description: >-
  After-command note that the host says it sent command-changed files but they
  have not landed here yet, so read them on the host.
ccVersion: 2.1.251
variables:
  - SYSTEM_REMINDER_DIR_SYNC_AFTER_COMMAND_NOT_SEEN_VAR_0
  - SYSTEM_REMINDER_DIR_SYNC_AFTER_COMMAND_NOT_SEEN_VAR_1
-->
${SYSTEM_REMINDER_DIR_SYNC_AFTER_COMMAND_NOT_SEEN_VAR_0} says it sent what that command changed, but it has not reached this session yet; it is taken in when it lands — until then, read those files on ${SYSTEM_REMINDER_DIR_SYNC_AFTER_COMMAND_NOT_SEEN_VAR_0} (the ${SYSTEM_REMINDER_DIR_SYNC_AFTER_COMMAND_NOT_SEEN_VAR_1} argument).
