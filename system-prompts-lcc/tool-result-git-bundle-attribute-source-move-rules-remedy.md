<!--
name: 'Tool result: git bundle attribute source move rules remedy'
description: >-
  Remedy telling the user to move attribute rules into a committed
  .gitattributes and remove the setting before retrying.
ccVersion: 2.1.282
variables:
  - TOOL_RESULT_GIT_BUNDLE_ATTRIBUTE_SOURCE_MOVE_RULES_REMEDY_VAR_0
-->
Move the attribute rules it points to into a .gitattributes file committed with the project first, then remove the setting from ${TOOL_RESULT_GIT_BUNDLE_ATTRIBUTE_SOURCE_MOVE_RULES_REMEDY_VAR_0} and retry.
