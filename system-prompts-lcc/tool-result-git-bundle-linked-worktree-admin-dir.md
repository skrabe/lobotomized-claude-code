<!--
name: 'Tool Result: Git Bundle Linked Worktree Admin Dir'
description: >-
  bXo.admin_dir clause interpolated into the linked-worktree git-bundle refusal
  when .git does not name the worktree admin directory.
ccVersion: 2.1.247
-->
whose .git file does not name the administrative directory git uses for it (or that directory is not where git keeps one, under the repository’s .git/worktrees).
