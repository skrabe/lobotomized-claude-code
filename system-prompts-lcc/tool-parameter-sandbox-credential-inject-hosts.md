<!--
name: 'Tool parameter: Sandbox credential inject hosts'
description: >-
  Model-facing inputSchema param description fragment (.describe on injectHosts)
  about where the proxy injects the credential relative to
  network.allowedDomains.
ccVersion: 2.1.199
-->
every reachable host. Each entry must be reachable via `network.allowedDomains` (sandbox-runtime validates this).
