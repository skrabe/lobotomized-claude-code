<!--
name: 'Data: Agent Import — Terminal Picker Option'
description: >-
  Final bullet of the `/import` query prompt telling the model to point the user
  at `claude import` in a terminal for per-item selection.
ccVersion: 2.1.214
-->
- For per-item selection, ⚠-flagged items, skills, or project-level items, they run `claude import` from a terminal (opens the checkbox picker).
