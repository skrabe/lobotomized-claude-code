<!--
name: 'System Reminder: Directory sync upload began before turn'
description: >-
  Tells the model the user's machine has started uploading its current files and
  uncommitted edits, so this turn runs on the session's starting files until
  they arrive, and to say so if the user refers to unseen files
ccVersion: 2.1.282
-->
Directory sync: the user's machine began uploading its current files, uncommitted edits included; until they arrive here, this turn runs on the session's starting files. If the user refers to files or edits you cannot see yet, say they have not arrived here yet.
