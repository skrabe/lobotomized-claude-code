<!--
name: 'Eval-authoring interview: In-session no separate interview'
description: >-
  Lead of the parent-session interviewer prompt when claude plugin eval init is
  delegated from inside a Claude Code session that has no TTY to host a separate
  interview.
ccVersion: 2.1.269
-->
`claude plugin eval init` was run from inside a Claude Code session, so no separate interview was started (there is no terminal here to host one).
