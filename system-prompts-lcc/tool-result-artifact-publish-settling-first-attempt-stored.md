<!--
name: 'Tool Result: Artifact publish settling — first attempt stored'
description: >-
  Reason clause gn() renders into the artifact publish 409 publish_settling
  tool_result, telling the model the first publish already reached claude.ai and
  is still being stored, so a retry is not a fresh publish.
ccVersion: 2.1.273
-->
the first attempt reached claude.ai and its content is still being stored
