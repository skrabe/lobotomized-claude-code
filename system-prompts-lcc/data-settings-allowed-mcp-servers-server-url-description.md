<!--
name: 'Data: allowedMcpServers.serverUrl setting description'
description: >-
  Description of the `allowedMcpServers.serverUrl` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
URL pattern with wildcard support (e.g., "https://*.example.com/*") for allowed remote MCP servers
