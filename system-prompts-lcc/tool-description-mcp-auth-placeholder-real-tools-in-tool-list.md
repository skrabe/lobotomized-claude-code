<!--
name: 'Tool Description: MCP auth placeholder real tools in tool list'
description: >-
  Tool-error suffix saying the MCP auth placeholder is obsolete: the server has
  authenticated and its real tools are already in the tool list, so the model
  should call those.
ccVersion: 2.1.281
variables:
  - TOOL_DESCRIPTION_MCP_AUTH_PLACEHOLDER_REAL_TOOLS_IN_TOOL_LIST_VAR_0
-->
. The MCP server '${TOOL_DESCRIPTION_MCP_AUTH_PLACEHOLDER_REAL_TOOLS_IN_TOOL_LIST_VAR_0.serverName}' has since been authenticated and its real tools are now in your tool list; call those instead of this placeholder.
