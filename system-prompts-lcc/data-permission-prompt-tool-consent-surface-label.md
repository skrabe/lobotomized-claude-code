<!--
name: 'Data: Permission-prompt-tool consent surface label'
description: >-
  Surface label interpolated into hdo()'s {behavior:"deny", message:'Permission
  for X requires the user to read a consent disclosure … and <label> cannot
  display it.'} — a denial message returned to the model as the tool result.
ccVersion: 2.1.214
-->
the configured --permission-prompt-tool (a tool_name+input wire)
