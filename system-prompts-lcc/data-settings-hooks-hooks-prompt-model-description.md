<!--
name: 'Data: hooks.hooks.(prompt).model setting description'
description: >-
  Description of the `hooks.hooks.(prompt).model` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Model to use for this prompt hook (e.g., "claude-sonnet-5"). If not specified, uses the default small fast model.
