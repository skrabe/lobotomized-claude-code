<!--
name: 'Slash Command: /cloud-plugins — org and repo plugins only'
description: >-
  Default /cloud-plugins consent state when no pin is stored: cloud sessions
  from this machine keep loading only the repository's and the organization's
  plugins. Renders into the local-jsx onDone string.
ccVersion: 2.1.246
-->
cloud sessions from this machine keep loading only the repository’s and your organization’s plugins
