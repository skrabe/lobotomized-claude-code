<!--
name: 'Tool Result: Cloud Session File Sync Repo Setting Steers Git'
description: >-
  First-upload refusal line when a settings file inside the repository sets a
  variable file sync's git would have to obey; tells the model to remove or move
  it and run claude --cloud again.
ccVersion: 2.1.282
variables:
  - TOOL_RESULT_CLOUD_SESSION_FILE_SYNC_REPO_SETTING_STEERS_GIT_VAR_0
-->
A settings file inside this repository sets ${TOOL_RESULT_CLOUD_SESSION_FILE_SYNC_REPO_SETTING_STEERS_GIT_VAR_0.name}, which file sync's git in this working tree would have to obey. Remove it, or move it to your user settings if you set it, then run claude --cloud again.
