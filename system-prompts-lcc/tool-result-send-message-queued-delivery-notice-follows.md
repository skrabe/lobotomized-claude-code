<!--
name: SendMessage Queued — Delivery Notice Follows
description: >-
  SendMessage success-message suffix when a peer inbox is bound: the item is
  queued and a [Cross-session delivery notice] follows if that session holds or
  refuses it.
ccVersion: 2.1.273
-->
; queued there — a [Cross-session delivery notice] follows if that session holds it (different permission mode: its user must approve first) or refuses it
