<!--
name: 'System Reminder: Dir sync halt line — start snapshot gone'
description: >-
  Halt line sent to the cloud session when the laptop's starting snapshot for a
  folder session is gone from its local sync store, so only a new cloud session
  from that folder can sync again.
ccVersion: 2.1.282
-->
file sync's starting snapshot for this folder session is gone from the user's machine (its local sync store there was removed or cleaned up, or the snapshot discarded as damaged); only a new cloud session from that folder syncs again
