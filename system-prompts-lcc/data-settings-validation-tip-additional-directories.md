<!--
name: 'Data: settings validation tip (additionalDirectories not an array)'
description: >-
  Suggestion appended to a settings.json validation error (additionalDirectories
  not an array); the model reads it in the tool result when its edit to a
  settings file fails validation.
ccVersion: 2.1.276
-->
Must be an array of directory paths. Example: ["~/projects", "/tmp/workspace"]. You can also use --add-dir flag or /add-dir command
