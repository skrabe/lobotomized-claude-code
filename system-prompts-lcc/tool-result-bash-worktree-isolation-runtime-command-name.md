<!--
name: 'Tool Result: Bash Worktree Isolation Runtime Command Name'
description: >-
  Worktree-isolation refusal when the command name is computed at runtime so it
  cannot be shown not to be git.
ccVersion: 2.1.257
variables:
  - TOOL_RESULT_BASH_WORKTREE_ISOLATION_RUNTIME_COMMAND_NAME_VAR_0
-->
runs a command whose name is computed at runtime ${TOOL_RESULT_BASH_WORKTREE_ISOLATION_RUNTIME_COMMAND_NAME_VAR_0}, so it cannot be shown not to be git
