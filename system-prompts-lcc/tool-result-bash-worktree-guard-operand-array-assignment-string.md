<!--
name: 'Worktree guard operand: string re-parsed as array assignment'
description: >-
  Reason clause passed to the B_() result builder: with a string it would
  re-parse as an array assignment.
ccVersion: 2.1.257
-->
with a string it would re-parse as an array assignment
