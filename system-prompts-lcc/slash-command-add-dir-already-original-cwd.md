<!--
name: 'Slash Command: /add-dir — already the current working directory'
description: >-
  Tells the model the path is the session's original cwd and therefore already
  readable, so no permission change happened.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_ADD_DIR_ALREADY_ORIGINAL_CWD_VAR_0
-->
${SLASH_COMMAND_ADD_DIR_ALREADY_ORIGINAL_CWD_VAR_0} is already the current working directory.
