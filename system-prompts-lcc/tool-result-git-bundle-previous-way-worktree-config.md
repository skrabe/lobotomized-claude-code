<!--
name: Git Bundle Previous Way — Per-Worktree Configuration
description: >-
  Reason clause naming extensions.worktreeConfig, which git sparse-checkout
  sets, as why the previous-way upload is also refused.
ccVersion: 2.1.280
-->
its repository has per-worktree configuration switched on (extensions.worktreeConfig, which git sparse-checkout sets)
