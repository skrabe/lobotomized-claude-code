<!--
name: 'Tool Result: Worktree git guard — exported var re-assigned with prefix'
description: >-
  Refusal clause: an exported var is re-assigned with a per-command prefix that
  can append onto the exported value at runtime.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_EXPORT_PREFIX_APPEND_VAR_0
-->
re-assigns the exported ${TOOL_RESULT_BASH_WORKTREE_GIT_EXPORT_PREFIX_APPEND_VAR_0} with a per-command prefix, which can append onto the exported value at runtime
