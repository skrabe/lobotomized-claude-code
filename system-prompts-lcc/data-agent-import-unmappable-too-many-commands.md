<!--
name: 'Data: Agent Import — Too Many Commands'
description: >-
  Unmappable `reason` when a source has more command files than the import scan
  cap; interpolated into the /import summary prompt.
ccVersion: 2.1.265
-->
Has more command files than the import scans. Review the remaining commands manually.
