<!--
name: 'Slash Command: /mcp — Already Reconnecting'
description: >-
  Tells the model the named server is already in pending reconnect, so another
  reconnect is a no-op while retries continue.
ccVersion: 2.1.251
variables:
  - SLASH_COMMAND_MCP_ALREADY_RECONNECTING_VAR_0
  - SLASH_COMMAND_MCP_ALREADY_RECONNECTING_VAR_1
-->
"${SLASH_COMMAND_MCP_ALREADY_RECONNECTING_VAR_0(SLASH_COMMAND_MCP_ALREADY_RECONNECTING_VAR_1)}" is already reconnecting — retries can take a few minutes when a server keeps failing.
