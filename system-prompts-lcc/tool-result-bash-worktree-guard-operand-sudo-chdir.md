<!--
name: 'Worktree Guard Operand: Sudo --chdir'
description: >-
  Gl("sudo") refusal when sudo --chdir/-D is present, so paths judged so far
  were read against the shell directory.
ccVersion: 2.1.259
-->
with --chdir: the paths judged so far were read against the shell directory
