<!--
name: 'Tool Result: Artifact Publish Host Grant Bounds Carry Forward'
description: >-
  Publish tool_result tail when stored capabilities cannot be read and the
  local-server grant still bounds what a republish would carry forward.
ccVersion: 2.1.273
-->
this computer's local-server grant bounds what a publish carries forward, so this publish cannot proceed without it. This is usually transient: retry.
