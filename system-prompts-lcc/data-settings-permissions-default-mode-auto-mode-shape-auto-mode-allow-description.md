<!--
name: >-
  Data: permissions.defaultMode.autoMode.shape.autoMode.allow setting
  description
description: >-
  Description of the `permissions.defaultMode.autoMode.shape.autoMode.allow`
  setting in Claude Code's settings JSON schema. The model reads it through
  /update-config and settings validation errors; it is also shown to users in
  the settings help.
ccVersion: 2.1.276
-->
Rules for the auto mode classifier allow section. Include the literal string "$defaults" to inherit the built-in rules at that position.
