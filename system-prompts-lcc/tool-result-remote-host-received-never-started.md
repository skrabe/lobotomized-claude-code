<!--
name: 'Tool Result: Remote Host Received Never Started'
description: >-
  still_running tool_result when the host had admitted the call but nothing had
  started, so whether it then ran is unknown.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_REMOTE_HOST_RECEIVED_NEVER_STARTED_VAR_0
  - TOOL_RESULT_REMOTE_HOST_RECEIVED_NEVER_STARTED_VAR_1
-->
${TOOL_RESULT_REMOTE_HOST_RECEIVED_NEVER_STARTED_VAR_0}${TOOL_RESULT_REMOTE_HOST_RECEIVED_NEVER_STARTED_VAR_1==="unasked"?" —":""} it had received the call but nothing had started yet; whether it then ran is not known. Check on its effect before repeating it, or ask the user.
