<!--
name: 'Tool result: git bundle attributes file full path remedy'
description: >-
  Remedy telling the user to set core.attributesFile to a full or ~/ path and
  retry.
ccVersion: 2.1.282
-->
Set it to a full path (on Windows, one that starts with a drive letter) or a ~/ path, then retry.
