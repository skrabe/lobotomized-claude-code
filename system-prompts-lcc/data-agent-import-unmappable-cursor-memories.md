<!--
name: 'Data: Agent Import — Cursor Memories'
description: >-
  Hardcoded unmappable `reason` for Cursor Memories (app-data, no file export);
  interpolated into the /import summary prompt.
ccVersion: 2.1.265
-->
Stored in Cursor app-data with no file-based export. Open Cursor → Settings → Rules & Memories and copy anything you need into CLAUDE.md.
