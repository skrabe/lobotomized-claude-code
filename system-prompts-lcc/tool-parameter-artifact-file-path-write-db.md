<!--
name: 'Tool Parameter: Artifact file_path — write_db Use'
description: >-
  Clause appended to the file_path parameter description explaining its meaning
  for write_db set/update as an alternative to inline `data`.
ccVersion: 2.1.237
-->
 For 'write_db' (db_op 'set' or 'update'), a local JSON file whose top-level object is sent as the document — an alternative to inline `data`, so a large document need not pass through the conversation.
