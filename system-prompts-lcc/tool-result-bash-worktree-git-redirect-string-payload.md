<!--
name: 'Tool Result: Worktree git basename string payload'
description: >-
  Deny-reason fragment when a worktree-isolated command runs a string through
  basename that can't be verified to stay inside the worktree.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_REDIRECT_STRING_PAYLOAD_VAR_0
  - TOOL_RESULT_BASH_WORKTREE_GIT_REDIRECT_STRING_PAYLOAD_VAR_1
-->
runs a string through ${TOOL_RESULT_BASH_WORKTREE_GIT_REDIRECT_STRING_PAYLOAD_VAR_0(TOOL_RESULT_BASH_WORKTREE_GIT_REDIRECT_STRING_PAYLOAD_VAR_1)}, which can't be verified to stay inside the worktree
