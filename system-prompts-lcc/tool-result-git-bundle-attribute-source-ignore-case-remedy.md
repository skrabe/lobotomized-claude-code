<!--
name: 'Tool result: git bundle core.ignoreCase remedy'
description: >-
  Remedy telling the user to move core.ignoreCase into .git/config or
  ~/.gitconfig, then retry.
ccVersion: 2.1.282
-->
Move the core.ignoreCase line into this repository’s .git/config or directly into your ~/.gitconfig, then retry.
