<!--
name: 'Tool Result: Artifact file must be a Buffer'
description: >-
  Artifact publish error telling the model a binary file's content must be
  passed as a raw-byte Buffer, not a string.
ccVersion: 2.1.218
-->
is binary — pass its content as a Buffer (raw bytes), not a string
