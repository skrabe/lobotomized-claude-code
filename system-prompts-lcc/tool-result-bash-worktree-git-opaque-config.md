<!--
name: 'Tool Result: Worktree git guard — opaque env injects git config'
description: >-
  Refusal clause: an opaque env assignment injects git configuration whose
  effect on where git writes can't be verified.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_OPAQUE_CONFIG_VAR_0
-->
sets ${TOOL_RESULT_BASH_WORKTREE_GIT_OPAQUE_CONFIG_VAR_0.opaque}, injecting git configuration whose effect on where git writes can't be verified
