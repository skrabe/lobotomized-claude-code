<!--
name: 'Data: Agent Import — Too Many MCP Servers'
description: >-
  Unmappable `reason` when a source lists more MCP servers than the import scan
  cap; interpolated into the /import summary prompt.
ccVersion: 2.1.265
-->
Has more MCP servers than the import scans. Review the remaining servers manually.
