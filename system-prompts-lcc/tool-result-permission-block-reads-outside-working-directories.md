<!--
name: 'Tool Result: Block Reads Outside Working Directories'
description: >-
  checkPermissions deny message when the user opts into
  permissions.blockReadsOutsideWorkingDirectories; becomes the tool_result on
  deny.
ccVersion: 2.1.257
-->
The user chose to block reads outside the working directories (permissions.blockReadsOutsideWorkingDirectories). Ask the user to add the directory with /add-dir, or to remove that setting.
