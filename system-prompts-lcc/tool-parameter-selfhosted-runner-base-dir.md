<!--
name: 'Tool Parameter: Self-hosted runner base_dir'
description: >-
  Input-schema description of the self-hosted runner start tool's `base_dir`
  parameter.
ccVersion: 2.1.224
variables:
  - TOOL_PARAMETER_SELFHOSTED_RUNNER_BASE_DIR_VAR_0
-->
Always passed to the runner (its built-in default of /workspace is unwritable on laptops). Default: ${TOOL_PARAMETER_SELFHOSTED_RUNNER_BASE_DIR_VAR_0}
