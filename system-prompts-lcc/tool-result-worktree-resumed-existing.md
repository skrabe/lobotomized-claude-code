<!--
name: 'Tool result: worktree resumed as-is'
description: >-
  EnterWorktree tool-result message fragment shown when a same-named worktree
  already existed and was resumed rather than reset.
ccVersion: 2.1.210
-->
 A worktree with this name already existed and was resumed as-is — it may carry an earlier session’s commits. Pass a different name if you wanted a fresh worktree.
