<!--
name: 'Tool Description: Artifact write_db Local File Ask'
description: >-
  write_db description clause that a write reading a local file asks every time,
  while inline data keeps the session-wide approval.
ccVersion: 2.1.246
-->
a write that reads a local file asks on its own every time and approving covers only it; writes of inline data keep the session-wide approval
