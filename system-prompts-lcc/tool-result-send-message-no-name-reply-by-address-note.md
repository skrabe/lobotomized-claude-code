<!--
name: Sender has no name — reply by address note
description: >-
  Note appended to a successful SendMessage result telling Claude the sending
  session has no name yet, so the receiver can answer only by the address on the
  message.
ccVersion: 2.1.234
-->
; note: this session has no name yet, so the receiver can answer only by the address on the message, not by name
