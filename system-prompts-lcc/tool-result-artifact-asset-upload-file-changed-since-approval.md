<!--
name: 'Tool Result: Artifact asset upload file changed since approval'
description: >-
  Preflight error (RZe) telling the model the approved file_path no longer names
  the same file and the upload must be retried so it is re-checked.
ccVersion: 2.1.234
-->
file_path no longer names the file that was approved (it moved, was replaced, or was rewritten) — retry the upload so it is checked again
