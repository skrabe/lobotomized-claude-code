<!--
name: 'Tool Result: Git Bundle Git File Unfollowed'
description: >-
  tC.git_file clause refusing the upload when .git is a pointer file this upload
  could not follow.
ccVersion: 2.1.280
-->
its .git entry is a pointer file this upload could not follow, so where it leads cannot be judged — start from an ordinary clone instead
