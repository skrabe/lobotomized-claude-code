<!--
name: 'Data: Background Session Blocked — Home Directory Trust Is Per-Session'
description: >-
  Refusal returned when a background or forked session would start in the home
  directory, which is trusted only one session at a time. It reaches the model
  through the /fork command result.
ccVersion: 2.1.281
-->
Workspace not trusted. The home directory is trusted one session at a time — start this from an interactive terminal there, or from a project directory.
