<!--
name: 'Plugin Validate: Type Contract Export Types Only'
description: Type-contract scan refusal when a top-level export is not type or interface.
ccVersion: 2.1.273
-->
`export` at the top level is followed by `type` or `interface`: a contract exports types and nothing else
