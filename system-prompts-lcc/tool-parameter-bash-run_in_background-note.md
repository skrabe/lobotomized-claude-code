<!--
name: 'Tool Parameter: Bash run_in_background note'
description: >-
  Notes that Bash commands can use run_in_background when the result is not
  needed immediately
ccVersion: 2.1.178
-->
  - Use `run_in_background` to run the command in the background when you don't need the result immediately; you'll be notified when it finishes.
