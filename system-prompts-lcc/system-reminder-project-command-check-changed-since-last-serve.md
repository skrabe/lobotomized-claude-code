<!--
name: 'System Reminder: Project Command Check Changed Since Last Serve'
description: >-
  One-time hook context: the project's command check differs from the one the
  computer last recorded for this project, so each command asks the person for
  this session and the check is accepted once the session's use of that computer
  ends
ccVersion: 2.1.277
-->
This project's command check is not the one that computer recorded the last time it served this project, so it is not run for this session and each command asks the person instead. It is accepted as the project's check once this session's use of that computer ends.
