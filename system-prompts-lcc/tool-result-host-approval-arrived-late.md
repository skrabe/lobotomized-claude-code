<!--
name: 'Tool Result: Host Approval Arrived Late'
description: >-
  Error outcome when a host approval reaches a tool after it stopped waiting, so
  nothing ran.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_HOST_APPROVAL_ARRIVED_LATE_VAR_0
-->
Your approval reached ${TOOL_RESULT_HOST_APPROVAL_ARRIVED_LATE_VAR_0.name} after it stopped waiting for it; nothing ran. Retry the call if it is still wanted.
