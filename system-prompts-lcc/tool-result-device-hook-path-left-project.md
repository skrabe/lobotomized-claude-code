<!--
name: 'Tool Result: Device Hook Path Left Project'
description: >-
  PreToolUse deny reason when a path named by the event moved outside the
  project while the hook waited for a slot.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_DEVICE_HOOK_PATH_LEFT_PROJECT_VAR_0
-->
not run — a path the event names moved outside the project on ${TOOL_RESULT_DEVICE_HOOK_PATH_LEFT_PROJECT_VAR_0.displayName} while it waited; retry
