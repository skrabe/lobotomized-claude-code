<!--
name: 'Slash Command: /plugin Install Cause — Version Folder Stayed Busy'
description: >-
  Cause clause in the failed plugin-version install error when the new copy or
  the version folder stayed busy during the install (a scanner reading fresh
  files, another program, or another process re-creating it).
ccVersion: 2.1.277
variables:
  - SLASH_COMMAND_PLUGIN_INSTALL_VERSION_FOLDER_BUSY_CAUSE_VAR_0
-->
the new copy or the version folder stayed busy while the install ran (${SLASH_COMMAND_PLUGIN_INSTALL_VERSION_FOLDER_BUSY_CAUSE_VAR_0}) — usually a scanner still reading the freshly downloaded files, another program using that folder, or another process re-creating it
