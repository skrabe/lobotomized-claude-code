<!--
name: 'Slash Command: /add-dir — already a working directory'
description: >-
  Tells the model the directory is already in the working-directory set, so the
  add was a no-op and access was already available.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_ADD_DIR_ALREADY_ADDED_VAR_0
-->
${SLASH_COMMAND_ADD_DIR_ALREADY_ADDED_VAR_0} is already added as a working directory.
