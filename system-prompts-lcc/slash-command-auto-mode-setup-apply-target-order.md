<!--
name: 'Slash Command: /auto-mode-setup --apply-target Ordering'
description: >-
  Flag-ordering usage error returned by /auto-mode-setup when --apply-target
  appears after --apply-file; returned to the model as the command's JSON
  result.
ccVersion: 2.1.218
-->
--apply-target must come before --expect-sha256 and --apply-file — not after --apply-file.
