<!--
name: 'Tool Result: Git Bundle Git File Unfollowed Pointer'
description: >-
  Previous-way clause refusing the upload when .git is a pointer file that is
  not the single gitdir line git writes.
ccVersion: 2.1.280
-->
its .git entry is a pointer file this upload could not follow (not the one gitdir line git writes), so where it leads cannot be judged — start from an ordinary clone instead
