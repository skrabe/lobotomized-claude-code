<!--
name: 'Tool Result: Computer Use App Partial Type Do Not Retype'
description: >-
  Warns that part of a string was already typed before a refusal, so the model
  must not re-type the whole string.
ccVersion: 2.1.246
-->
a fresh app_screenshot and do NOT re-type the whole string (that would append on top of the prefix). Type only the remainder, or clear the field first.
