<!--
name: 'Tool Result: MCP Server Needs Re-authentication'
description: >-
  McpAuthError message surfaced as the MCP tool call's error result when the
  server needs the user to sign in again via /mcp.
ccVersion: 2.1.273
variables:
  - TOOL_RESULT_MCP_SERVER_NEEDS_REAUTH_VAR_0
-->
MCP server "${TOOL_RESULT_MCP_SERVER_NEEDS_REAUTH_VAR_0}" needs you to sign in again (run /mcp to re-authenticate)
