<!--
name: 'Tool Parameter: AppifactRepl Artifact'
description: >-
  Input-schema description of AppifactRepl's optional artifact URL or id bound
  for claude.use("db").
ccVersion: 2.1.273
-->
The existing artifact the code works on (its URL or id), bound for claude.use("db"). Omit it only when a named skill's code creates a new artifact.
