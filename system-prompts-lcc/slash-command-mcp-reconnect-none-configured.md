<!--
name: 'MCP reconnect: none configured'
description: >-
  /mcp reconnect result reported when the requested servers aren't configured
  yet; returned as model-visible local-command-stdout.
ccVersion: 2.1.210
variables:
  - SLASH_COMMAND_MCP_RECONNECT_NONE_CONFIGURED_VAR_0
-->
${SLASH_COMMAND_MCP_RECONNECT_NONE_CONFIGURED_VAR_0} MCP server(s) aren't configured yet, so there's nothing to reconnect. The rest are already connected or connecting.
