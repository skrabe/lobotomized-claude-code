<!--
name: 'Tool Result: Cannot background — session persistence disabled'
description: >-
  Tells the model /background cannot fork a job because session persistence is
  off, so the forked job would have nothing to resume.
ccVersion: 2.1.233
-->
Cannot background — session persistence is disabled, so the forked job would have nothing to resume.
