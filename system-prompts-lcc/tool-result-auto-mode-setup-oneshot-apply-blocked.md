<!--
name: 'Tool result: auto-mode-setup one-shot --apply blocked'
description: >-
  Auto-mode-setup non-interactive command result telling the invoking model
  one-shot --apply is unavailable and to use --propose then --apply-file after
  user review.
ccVersion: 2.1.210
-->
One-shot --apply isn’t available (it would write model output with no review). Use --propose, show the result to the user, then --apply-file <path>.
