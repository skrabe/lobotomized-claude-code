<!--
name: 'Slash Command: Plugin Uninstall — Settings Source Is the --settings File'
description: >-
  Yao() clause for a --settings file that is on disk now and that later sessions
  would read, telling the user to switch the plugin off there; part of the
  uninstall refusal.
ccVersion: 2.1.282
variables:
  - SLASH_COMMAND_PLUGIN_UNINSTALL_SOURCE_FLAG_SETTINGS_FILE_VAR_0
-->
${SLASH_COMMAND_PLUGIN_UNINSTALL_SOURCE_FLAG_SETTINGS_FILE_VAR_0} (the --settings file as it is now, which a later session started with it would read: switch it off there)
