<!--
name: 'Tool Result: Git Bundle Git File Second Name'
description: >-
  Previous-way clause refusing the upload when the .git pointer file has a
  second hard-link name.
ccVersion: 2.1.280
-->
its .git pointer file has a second name (a hard link), so what it names cannot be trusted between this read and git’s — remove the other name, or start from an ordinary clone
