<!--
name: 'Tool Parameter: artifact db ids'
description: >-
  Schema description of the artifact data tool's ids parameter: for action
  'profiles' only, the 1-N people ids to name, exactly as shown ('u_' plus 22
  characters).
ccVersion: 2.1.281
variables:
  - TOOL_PARAMETER_ARTIFACT_DB_IDS_VAR_0
-->
action 'profiles' only: the people to name, 1-${TOOL_PARAMETER_ARTIFACT_DB_IDS_VAR_0} ids exactly as a document or live event showed them ("u_" plus 22 characters).
