<!--
name: 'Tool Result: Workflow script exceeds the byte limit'
description: >-
  Tells the model its workflow script was rejected for size, so the fix is to
  shrink the script rather than retry it unchanged.
ccVersion: 2.1.233
variables:
  - TOOL_RESULT_WORKFLOW_SCRIPT_EXCEEDS_SIZE_LIMIT_VAR_0
-->
Script exceeds ${TOOL_RESULT_WORKFLOW_SCRIPT_EXCEEDS_SIZE_LIMIT_VAR_0} bytes
