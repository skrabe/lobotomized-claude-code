<!--
name: Bash Background Session Ending
description: >-
  Bash tool-result text when the session is ending: the background command is
  stopped at turn end so its result only arrives while the model is still
  working.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_BASH_BACKGROUND_SESSION_ENDING_VAR_0
-->
This session takes no further input, though: the command is stopped when your turn ends, so its result reaches you only while you are still working. ${TOOL_RESULT_BASH_BACKGROUND_SESSION_ENDING_VAR_0()}
