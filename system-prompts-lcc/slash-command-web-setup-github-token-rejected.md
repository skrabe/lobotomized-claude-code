<!--
name: 'Slash Command: /web-setup — GitHub rejected the token'
description: Tells the model GitHub rejected the token and names gh auth login as the fix.
ccVersion: 2.1.233
-->
GitHub rejected that token. Run `gh auth login` and try again.
