<!--
name: 'Slash Command: Plugin Install — Invalid npm Plugin Id'
description: >-
  Hkt() reason for an @npm plugin id whose first half is not a package name the
  npm marketplace accepts; interpolated into the invalid-id install refusal.
ccVersion: 2.1.282
-->
its first half is not a package name that the npm marketplace accepts.
