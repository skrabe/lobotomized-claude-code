<!--
name: 'Plugin Validate: MCP URL Cleartext Warning (Lead)'
description: >-
  First half of the plugin-validate warning that an MCP server url uses http://
  or ws:// to a non-loopback host, so requests, headers and credentials travel
  in cleartext.
ccVersion: 2.1.281
variables:
  - SLASH_COMMAND_PLUGIN_VALIDATE_MCP_URL_CLEARTEXT_LEAD_VAR_0
-->
url uses ${SLASH_COMMAND_PLUGIN_VALIDATE_MCP_URL_CLEARTEXT_LEAD_VAR_0}:// to a non-loopback host — requests, headers and any 
