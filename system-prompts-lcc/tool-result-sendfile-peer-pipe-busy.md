<!--
name: SendFile peer pipe busy
description: >-
  Suffix appended to a SendFile failure telling the model the peer pipe is
  momentarily busy and to retry.
ccVersion: 2.1.210
-->
 — the peer is alive but its pipe is momentarily busy. Retry the same address shortly.
