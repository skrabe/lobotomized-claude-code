<!--
name: Artifact Upload Asset Path Too Long
description: >-
  List-entry refusal when a path is longer than an approval can show, directing
  a solo file_path upload.
ccVersion: 2.1.276
variables:
  - TOOL_RESULT_ARTIFACT_UPLOAD_ASSET_PATH_TOO_LONG_VAR_0
-->
this path is longer than ${TOOL_RESULT_ARTIFACT_UPLOAD_ASSET_PATH_TOO_LONG_VAR_0} characters, as written or as an absolute path, which is more than an approval can show in full — upload that file by itself with \`file_path\`.
