<!--
name: 'Tool Description: MCP server needs reauthorize'
description: >-
  Tool-error suffix: the MCP server needs the user to re-authorize before its
  tools work again.
ccVersion: 2.1.251
variables:
  - TOOL_DESCRIPTION_MCP_SERVER_NEEDS_REAUTHORIZE_VAR_0
-->
. Its MCP server '${TOOL_DESCRIPTION_MCP_SERVER_NEEDS_REAUTHORIZE_VAR_0.serverName}' needs the user to re-authorize it before its tools work again. Continue without this tool and tell the user if the task depends on it.
