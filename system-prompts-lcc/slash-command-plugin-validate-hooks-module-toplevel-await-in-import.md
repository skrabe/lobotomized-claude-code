<!--
name: 'Plugin validate: top-level await in an imported file'
description: >-
  Hooks-module scan refusal when a file the entry imports uses top-level await;
  only the entry is linked as an async module.
ccVersion: 2.1.246
-->
a top-level await in a file the entry imports; the environment links the entry alone as an async module
