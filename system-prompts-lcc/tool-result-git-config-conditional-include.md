<!--
name: 'Tool Result: Git Config Conditional Include'
description: >-
  Explains git was not run because the repository config has an includeIf, so
  spawn-time config cannot be determined.
ccVersion: 2.1.274
-->
the repository's own git config has a conditional include (includeIf), so what a spawn there would load cannot be determined
