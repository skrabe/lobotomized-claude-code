<!--
name: 'Tool Result: set_cwd blocked-by-rule (hidden chars)'
description: >-
  Fallback message value in the set_cwd tool's rejected response when the
  blocking Cd rule text contains control/invisible characters and cannot be
  echoed; model reads it to act.
ccVersion: 2.1.201
-->
A Cd permission rule blocks this directory. The rule text contains control or invisible characters, so it is not echoed here — check the Cd(...) entries in your settings.
