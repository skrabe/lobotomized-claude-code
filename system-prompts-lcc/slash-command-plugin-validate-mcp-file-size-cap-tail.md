<!--
name: 'Slash command: /plugin validate MCP file cap (tail)'
description: >-
  Tail of the MCP-server-file refusal note giving the byte cap and telling the
  user to validate the real file separately.
ccVersion: 2.1.281
variables:
  - SLASH_COMMAND_PLUGIN_VALIDATE_MCP_FILE_SIZE_CAP_TAIL_VAR_0
-->
${SLASH_COMMAND_PLUGIN_VALIDATE_MCP_FILE_SIZE_CAP_TAIL_VAR_0} bytes. The plugin loader has neither limit, so validate the real file separately.
