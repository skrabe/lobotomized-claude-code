<!--
name: Orphaned background shell stopped explanation
description: >-
  Inner explanatory text of the orphaned background shell task-result block
  injected into model context on resume.
ccVersion: 2.1.265
variables:
  - SYSTEM_REMINDER_ORPHANED_SHELL_STOPPED_FRAMING_VAR_0
-->

<${SYSTEM_REMINDER_ORPHANED_SHELL_STOPPED_FRAMING_VAR_0}>${"No completion record was found for it in the previous session. It may have been stopped (via the UI, Monitor timeout, or agent teardown — these leave no transcript marker), or it may have been running when the previous Claude Code process exited. Check the output file for partial results before assuming it completed."}</${SYSTEM_REMINDER_ORPHANED_SHELL_STOPPED_FRAMING_VAR_0}>
