<!--
name: 'Slash Command: Workflow Launch Exec No Pending Handoff'
description: >-
  Result text of the hidden workflow-launch-exec command when no pending launch
  handoff exists (restored session replaying the dispatch turn); emitted as
  <local-command-stdout> and replayed into the model's context.
ccVersion: 2.1.218
-->
workflow-launch-exec: no pending launch handoff for this invocation (expected when a restored session replays the dispatch turn); nothing executed.
