<!--
name: 'Tool Result: Auto-mode classifier only - reason, session not in auto mode'
description: >-
  Reason clause passed as $9(e,n)'s n argument at 7086913, rendering into the
  catalogued deny message "Only the auto-mode classifier can allow ${tool}:
  ${reason}". A behavior:"deny" message is returned to the model as the
  tool_result.
ccVersion: 2.1.265
-->
the session is not in auto mode
