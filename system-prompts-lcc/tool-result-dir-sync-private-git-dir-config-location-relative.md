<!--
name: 'Tool result: dir sync private git dir config location relative'
description: >-
  Cause clause saying a config-locating setting is a relative path that git
  under file sync's private git directory would resolve from another directory.
ccVersion: 2.1.282
variables:
  - TOOL_RESULT_DIR_SYNC_PRIVATE_GIT_DIR_CONFIG_LOCATION_RELATIVE_VAR_0
-->
${TOOL_RESULT_DIR_SYNC_PRIVATE_GIT_DIR_CONFIG_LOCATION_RELATIVE_VAR_0} is a relative path, which git under the private git directory resolves from another directory
