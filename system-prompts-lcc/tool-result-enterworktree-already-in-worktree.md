<!--
name: Already in worktree session
description: >-
  EnterWorktree validation error returned to the model when already inside a
  worktree session.
ccVersion: 2.1.206
-->
Already in a worktree session. Pass `path` to switch into another existing worktree, or use ExitWorktree to leave this one before creating a new worktree.
