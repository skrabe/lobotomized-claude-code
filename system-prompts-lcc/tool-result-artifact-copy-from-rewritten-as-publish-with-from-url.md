<!--
name: Artifact copy_from Rewritten As Publish With from_url
description: >-
  g_/Wd() replacement that rewrites copy_from in deny messages when that action
  is not in this session's schema, so the model is told to publish with
  asset:true and from_url instead.
ccVersion: 2.1.261
-->
publish with `asset: true` and `from_url`
