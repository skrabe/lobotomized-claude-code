<!--
name: 'Plugin validate: state library source unreadable'
description: >-
  Hooks-module scan refusal when a state library read/update source is not a
  literal plugin/key reference
ccVersion: 2.1.281
variables:
  - >-
    SLASH_COMMAND_PLUGIN_VALIDATE_HOOKS_MODULE_STATE_LIBRARY_SOURCE_UNREADABLE_VAR_0
-->
the state library's ${SLASH_COMMAND_PLUGIN_VALIDATE_HOOKS_MODULE_STATE_LIBRARY_SOURCE_UNREADABLE_VAR_0} takes a source the scan can read: a reference whose plugin and key are string literals, or what atom, memberOf or derive (imported from "claude-code") made of one, written there or in a const of this file, so the values a module reads and writes can be listed
