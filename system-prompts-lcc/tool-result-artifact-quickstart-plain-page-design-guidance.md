<!--
name: 'Tool Result: Artifact Quickstart Plain Page Design Guidance'
description: >-
  Quickstart tool_result clause that inlines the artifact-design skill text for
  a plain page and tells the model not to load that skill again before writing
  and publishing.
ccVersion: 2.1.273
variables:
  - TOOL_RESULT_ARTIFACT_QUICKSTART_PLAIN_PAGE_DESIGN_GUIDANCE_VAR_0
-->


For a plain page, the page-design guidance follows. It is the \`artifact-design\` skill's own text, so do not load that skill as well. Write the page to a file, then publish it.

${TOOL_RESULT_ARTIFACT_QUICKSTART_PLAIN_PAGE_DESIGN_GUIDANCE_VAR_0}
