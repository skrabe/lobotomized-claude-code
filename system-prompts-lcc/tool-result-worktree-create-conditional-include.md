<!--
name: 'Tool Result: Worktree Create Conditional Include'
description: >-
  WorktreeIsolationError when includeIf makes checkout filter drivers
  unknowable; returned to the model as an EnterWorktree tool error.
ccVersion: 2.1.247
-->
The repository git config has a conditional include (includeIf), so the filter drivers a checkout would load cannot be determined
