<!--
name: 'Data: hooks.matcher setting description'
description: >-
  Description of the `hooks.matcher` setting in Claude Code's settings JSON
  schema. The model reads it through /update-config and settings validation
  errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
String pattern to match (e.g. tool names like "Write")
