<!--
name: 'Worktree Guard Operand: Program That Can Start Another Program'
description: >-
  Gl() refusal when a data:/URL/script payload matches n.bridge and sits next to
  a computed or non-plain operand, env, or input.
ccVersion: 2.1.259
variables:
  - TOOL_RESULT_BASH_WORKTREE_GUARD_OPERAND_BRIDGE_PROGRAM_VAR_0
-->
with a program that can start another program (${TOOL_RESULT_BASH_WORKTREE_GUARD_OPERAND_BRIDGE_PROGRAM_VAR_0[0]}) next to an operand, env value or input that is computed at runtime or not plain text
