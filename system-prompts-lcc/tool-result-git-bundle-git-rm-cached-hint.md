<!--
name: 'Tool Result: Git Bundle git rm --cached Hint'
description: >-
  Continuation of the index-added git-bundle refusal naming git rm --cached --
  <file> so the file stays on disk.
ccVersion: 2.1.237
-->
`git rm --cached -- <file>` (the file itself stays on disk) — if you did not add 
