<!--
name: Git bundle unborn branch in shallow clone
description: >-
  Bundle-failure result telling the model nothing can be bundled because the
  current branch has no commits and the shallow clone's other branches are cut
  short, and to commit first or run git fetch --unshallow.
ccVersion: 2.1.239
-->
The current branch has no commits yet, and the other branches of this shallow clone are cut short, so nothing here can be bundled; commit first, or deepen the clone (git fetch --unshallow)
