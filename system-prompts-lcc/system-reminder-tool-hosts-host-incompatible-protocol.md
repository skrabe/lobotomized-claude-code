<!--
name: 'Tool Hosts Notice: Incompatible Remote-Tool Protocol'
description: >-
  Per-host bullet when this session and the host share no remote-tool protocol
  version, so calls to it will fail until the older side is updated.
ccVersion: 2.1.246
variables:
  - SYSTEM_REMINDER_TOOL_HOSTS_HOST_INCOMPATIBLE_PROTOCOL_VAR_0
  - SYSTEM_REMINDER_TOOL_HOSTS_HOST_INCOMPATIBLE_PROTOCOL_VAR_1
-->
- ${SYSTEM_REMINDER_TOOL_HOSTS_HOST_INCOMPATIBLE_PROTOCOL_VAR_0}: attached, but its Claude Code and this session's share no remote-tool protocol version; calls to it will fail${SYSTEM_REMINDER_TOOL_HOSTS_HOST_INCOMPATIBLE_PROTOCOL_VAR_1} until the older side is updated.
