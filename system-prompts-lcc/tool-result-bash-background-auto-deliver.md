<!--
name: Bash Background Auto Deliver
description: >-
  Bash tool-result text telling the model a background command's result is
  delivered automatically and not to poll or sleep for it.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_BASH_BACKGROUND_AUTO_DELIVER_VAR_0
-->
When it finishes, its result is delivered to you automatically — ${TOOL_RESULT_BASH_BACKGROUND_AUTO_DELIVER_VAR_0} — so there is no need to check whether it is done or to sleep; if that result is all you are waiting for, end your turn.
