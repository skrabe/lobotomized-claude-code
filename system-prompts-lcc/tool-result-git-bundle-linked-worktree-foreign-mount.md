<!--
name: 'Tool result: git bundle linked worktree foreign mount'
description: >-
  Linked-worktree refusal clause when the worktree's administrative directory
  has something mounted over it.
ccVersion: 2.1.282
-->
whose administrative directory under the repository’s .git/worktrees has something mounted over it (another device or, on Linux, another mount) — unmount it first.
