<!--
name: 'Slash command: /plugin validate MCP schema issue suffix'
description: >-
  Wraps each MCP server schema issue message with a note that the plugin loader
  drops the server at load.
ccVersion: 2.1.281
variables:
  - SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_SCHEMA_ISSUE_DROPPED_VAR_0
-->
${SLASH_COMMAND_PLUGIN_VALIDATE_MCP_SERVER_SCHEMA_ISSUE_DROPPED_VAR_0.message}. The plugin loader silently drops this server at load.
