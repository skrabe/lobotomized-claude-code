<!--
name: 'Data: permissions.defaultMode.briefView.shape.defaultView setting description'
description: >-
  Description of the `permissions.defaultMode.briefView.shape.defaultView`
  setting in Claude Code's settings JSON schema. The model reads it through
  /update-config and settings validation errors; it is also shown to users in
  the settings help.
ccVersion: 2.1.276
-->
Default transcript view: chat (SendUserMessage checkpoints only) or transcript (full)
