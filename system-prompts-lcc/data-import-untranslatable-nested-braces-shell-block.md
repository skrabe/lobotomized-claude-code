<!--
name: 'Data: Import untranslatable nested braces in shell block'
description: >-
  Unmappable-item reason for a !{…} block with nested braces the importer would
  truncate, interpolated into the /import query prompt and the
  import-to-claude-code SKILL.md
ccVersion: 2.1.214
-->
Its `!{…}` shell block contains nested braces, which this importer would truncate.
