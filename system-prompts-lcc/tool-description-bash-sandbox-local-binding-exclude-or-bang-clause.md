<!--
name: 'Tool Description: Bash sandbox — local binding, exclude or `!` prefix clause'
description: >-
  Clause added to the Bash sandbox guidance on local port binding. It lists the
  user's other options: exclude the command with `/sandbox exclude`, or run it
  themselves with the `!` prefix.
ccVersion: 2.1.281
-->
, exclude the command with `/sandbox exclude <pattern>`, or run it themselves with the `!` prefix
