<!--
name: 'Slash Command: /config Plugin Saved Reload Required'
description: >-
  messageSuffix appended to /config stdout after a plugin option is saved but
  the plugin module did not reload.
ccVersion: 2.1.269
-->
 · saved, but the plugin did not reload; /reload-plugins applies it
