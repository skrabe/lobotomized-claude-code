<!--
name: 'System Reminder: MCP Servers Need Auth — Non-Interactive Lead'
description: >-
  Lead sentence interpolated as ${Pt} into the MCP needs-auth reminder when the
  session is non-interactive, saying Claude cannot run the OAuth flow here.
ccVersion: 2.1.282
-->
This session is non-interactive, so Claude cannot run the OAuth flow here.
