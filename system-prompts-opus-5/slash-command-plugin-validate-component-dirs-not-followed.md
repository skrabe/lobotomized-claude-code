<!--
name: 'Plugin validate: component dirs are not followed'
description: >-
  Error telling the caller to pass the real directory because component dirs are
  read without following symlinks.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_PLUGIN_VALIDATE_COMPONENT_DIRS_NOT_FOLLOWED_VAR_0
-->
${SLASH_COMMAND_PLUGIN_VALIDATE_COMPONENT_DIRS_NOT_FOLLOWED_VAR_0} Component directories are read without following symlinks, so nothing here was validated. Pass the real directory instead.
