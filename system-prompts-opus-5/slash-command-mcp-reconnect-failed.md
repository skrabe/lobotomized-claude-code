<!--
name: 'Slash Command: /mcp reconnect — reconnect failed'
description: >-
  Tells the model the server ended up cached/pending/failed/disabled rather than
  connected, so its tools are still unavailable.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_RECONNECT_FAILED_VAR_0
-->
Failed to reconnect to ${SLASH_COMMAND_MCP_RECONNECT_FAILED_VAR_0}
