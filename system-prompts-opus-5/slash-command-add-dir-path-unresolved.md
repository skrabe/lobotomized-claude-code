<!--
name: Slash Command Add Dir Path Unresolved
description: >-
  /add-dir local-jsx onDone text when the path cannot be resolved, wrapped as a
  user local-command-stdout message.
ccVersion: 2.1.251
variables:
  - SLASH_COMMAND_ADD_DIR_PATH_UNRESOLVED_VAR_0
-->
Path ${SLASH_COMMAND_ADD_DIR_PATH_UNRESOLVED_VAR_0} couldn't be resolved, so it can't be used as a working directory. Check the path or the settings entry that lists it.
