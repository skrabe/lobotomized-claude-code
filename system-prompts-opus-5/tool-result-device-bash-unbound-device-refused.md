<!--
name: 'Tool Result: Device Bash Unbound Device Refused'
description: >-
  Returned as an error tool result when device_bash is requested from a session
  that is not bound to the device.
ccVersion: 2.1.227
variables:
  - TOOL_RESULT_DEVICE_BASH_UNBOUND_DEVICE_REFUSED_VAR_0
-->
${TOOL_RESULT_DEVICE_BASH_UNBOUND_DEVICE_REFUSED_VAR_0} refused: this device is not bound to the session (it registered without a device id). Start the cloud session from this machine with claude --remote so it registers as a bound device.
