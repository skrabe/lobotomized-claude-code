<!--
name: Artifact List Type Empty Name
description: >-
  list-by-type validation error when type is empty, telling the model to pass a
  name or type_url.
ccVersion: 2.1.257
-->
`type` is empty — pass an Artifact type's name (as action "list_types" shows it), or its link as `type_url`
