<!--
name: 'Tool Parameter: Agent Model Override User-Explicit'
description: >-
  Gated extra sentence on the Agent model parameter telling the model to set it
  only when the user named a model.
ccVersion: 2.1.246
-->
 Set this only when the user explicitly asked for a specific model; otherwise omit it so the worker inherits the session model.
