<!--
name: 'Tool Result: Session Registry Unreadable'
description: >-
  Suffix on a SendMessage failure telling the model the local session registry
  could not be read and to retry the same address shortly.
ccVersion: 2.1.237
-->
 — this machine's session registry could not be read just now (a transient local condition). Retry the same address shortly.
