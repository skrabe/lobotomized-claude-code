<!--
name: 'Tool Result: app_type no focused text (overwrite blocks fallback)'
description: >-
  Fragment of the no_focused_text reason string interpolated into an isError
  app_* tool_result when overwrite_existing blocked the raw-keystroke fallback.
ccVersion: 2.1.246
-->
fallback (it can only insert at the caret). Drop it and click into the field first (double-click on canvas apps like Keynote), 
