<!--
name: 'Tool Result: Dir Sync Unreadable Word'
description: >-
  After-forward sessionNote when the host's dir_sync word is malformed, so
  post-command files arrive with the next user message.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_DIR_SYNC_UNREADABLE_WORD_VAR_0
-->
Directory sync: ${TOOL_RESULT_DIR_SYNC_UNREADABLE_WORD_VAR_0} answered about that command's files in a form this session could not read; files it changed there reach this session with the user's next message.
