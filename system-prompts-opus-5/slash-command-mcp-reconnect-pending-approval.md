<!--
name: 'Slash Command: /mcp — Reconnect Blocked Pending Approval'
description: >-
  Tells the model the server cannot be reconnected until the user approves it in
  the terminal /mcp panel.
ccVersion: 2.1.251
variables:
  - SLASH_COMMAND_MCP_RECONNECT_PENDING_APPROVAL_VAR_0
  - SLASH_COMMAND_MCP_RECONNECT_PENDING_APPROVAL_VAR_1
-->
"${SLASH_COMMAND_MCP_RECONNECT_PENDING_APPROVAL_VAR_0(SLASH_COMMAND_MCP_RECONNECT_PENDING_APPROVAL_VAR_1)}" is pending approval. Approve it with \`/mcp\` in the terminal first.
