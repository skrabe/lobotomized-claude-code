<!--
name: 'Tool Result: Artifact DB Write Symlink-Resolution User Approval Required'
description: >-
  safetyCheck decisionReason when write_db resolves outside the spelled path so
  the auto-permission classifier sees only the spelling.
ccVersion: 2.1.237
variables:
  - >-
    TOOL_RESULT_ARTIFACT_DB_WRITE_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_0
  - >-
    TOOL_RESULT_ARTIFACT_DB_WRITE_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_1
-->
Writing an artifact database document${TOOL_RESULT_ARTIFACT_DB_WRITE_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_0} sends it to a page every viewer can read, and the auto-permission classifier is shown only the path’s spelling, not where it resolves — approval must come from the user${TOOL_RESULT_ARTIFACT_DB_WRITE_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_1}
