<!--
name: 'Tool Result: Dir Sync Host Stopped Syncing'
description: >-
  go_with_note on the forwarded tool when the host has stopped syncing, so the
  command ran on files that may omit local edits.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_DIR_SYNC_HOST_STOPPED_SYNCING_VAR_0
-->
${TOOL_RESULT_DIR_SYNC_HOST_STOPPED_SYNCING_VAR_0} has stopped syncing this session's files; the command ran on its files as they are, which may not include your edits here
