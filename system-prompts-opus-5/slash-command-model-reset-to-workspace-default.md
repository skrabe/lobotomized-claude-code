<!--
name: 'Slash Command: /model — reset to the workspace default (cloud session)'
description: >-
  Tells the model that its per-session pin was cleared and the workspace default
  now applies, so it should not claim a specific pinned model.
ccVersion: 2.1.233
-->
Reset model to the workspace default
