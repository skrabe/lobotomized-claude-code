<!--
name: 'Tool Result: resume_replies denied in remote session'
description: >-
  Permission-decision message returned by checkPermissions for the Artifact
  tool's resume_replies action; the deny path sends it to the model as the tool
  result. Permission denial telling the model resume_replies is a live-session
  action and that a remote session should re-register with action "watch".
ccVersion: 2.1.234
-->
Resuming auto-replies is a live-session action; in a remote session comment wakes ride durable watch subscriptions — use action "watch" to re-register one. Nothing here needs approval.
