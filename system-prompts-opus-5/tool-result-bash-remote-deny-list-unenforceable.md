<!--
name: 'Tool Result: Bash Remote File Deny Lists Unenforceable'
description: >-
  Bash tool_result when a remote call carries additional file deny lists the
  local sandbox cannot enforce, so the command is not run.
ccVersion: 2.1.246
-->
This call carries file deny lists that cannot be enforced here yet. The command was not run.
