<!--
name: 'Tool Result: Artifact DB Write Hard-Link User Approval Required'
description: >-
  safetyCheck decisionReason when write_db would send a hard-linked file (other
  paths also name it) to a page every viewer can read.
ccVersion: 2.1.237
variables:
  - TOOL_RESULT_ARTIFACT_DB_WRITE_HARD_LINK_USER_APPROVAL_REQUIRED_VAR_0
  - TOOL_RESULT_ARTIFACT_DB_WRITE_HARD_LINK_USER_APPROVAL_REQUIRED_VAR_1
-->
Writing an artifact database document${TOOL_RESULT_ARTIFACT_DB_WRITE_HARD_LINK_USER_APPROVAL_REQUIRED_VAR_0} sends a file other paths also name to a page every viewer can read — approval must come from the user, not the auto-permission classifier${TOOL_RESULT_ARTIFACT_DB_WRITE_HARD_LINK_USER_APPROVAL_REQUIRED_VAR_1}
