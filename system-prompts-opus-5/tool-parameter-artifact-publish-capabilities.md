<!--
name: 'Tool Parameter: Artifact Publish Capabilities'
description: >-
  Artifact tool capabilities parameter: runtime capability map, requiring the
  artifact-capabilities skill.
ccVersion: 2.1.269
-->
publish: the runtime capabilities this page declares, as {name: config}. Claude loads the \`${TOOL_PARAMETER_ARTIFACT_PUBLISH_CAPABILITIES_VAR_0}\` skill before passing it. On a redeploy Claude omits the field to keep what the page has, and {} clears it.
