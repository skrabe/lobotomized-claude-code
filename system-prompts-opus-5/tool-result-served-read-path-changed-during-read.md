<!--
name: 'Tool Result: Served Read Path Changed During Read'
description: >-
  Refuses a served file read when the path changed where it points while bytes
  were being read.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_SERVED_READ_PATH_CHANGED_DURING_READ_VAR_0
-->
${TOOL_RESULT_SERVED_READ_PATH_CHANGED_DURING_READ_VAR_0} changed where it points while it was being read, so nothing was returned. Read it again.
