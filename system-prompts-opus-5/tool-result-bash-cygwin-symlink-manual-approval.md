<!--
name: Bash cygwin symlink manual approval
description: >-
  Permission message saying a path traverses a Cygwin-emulated symlink that Git
  Bash follows but Node does not, so it needs manual approval.
ccVersion: 2.1.232
-->
Path traverses a Cygwin-emulated symlink (Git Bash follows it, Node does not) — manual approval required
