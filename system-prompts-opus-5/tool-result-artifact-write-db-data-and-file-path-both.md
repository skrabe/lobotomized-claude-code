<!--
name: 'Tool Result: Artifact write_db data and file_path Both'
description: >-
  validateInput rejection when write_db set/update is given both `data` and
  `file_path`.
ccVersion: 2.1.237
variables:
  - TOOL_RESULT_ARTIFACT_WRITE_DB_DATA_AND_FILE_PATH_BOTH_VAR_0
-->
db_op "${TOOL_RESULT_ARTIFACT_WRITE_DB_DATA_AND_FILE_PATH_BOTH_VAR_0}" takes \`data\` or \`file_path\`, not both — remove one.
