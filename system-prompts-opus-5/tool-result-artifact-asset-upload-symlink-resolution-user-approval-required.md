<!--
name: Asset upload symlink-resolution approval reason
description: >-
  safetyCheck decisionReason when the classifier sees only the path spelling,
  not where the symlink resolves.
ccVersion: 2.1.234
variables:
  - >-
    TOOL_RESULT_ARTIFACT_ASSET_UPLOAD_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_0
  - >-
    TOOL_RESULT_ARTIFACT_ASSET_UPLOAD_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_1
-->
Uploading ${TOOL_RESULT_ARTIFACT_ASSET_UPLOAD_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_0} sends it to a web-reachable page, and the auto-permission classifier is shown only the path’s spelling, not where it resolves — approval must come from the user${TOOL_RESULT_ARTIFACT_ASSET_UPLOAD_SYMLINK_RESOLUTION_USER_APPROVAL_REQUIRED_VAR_1}
