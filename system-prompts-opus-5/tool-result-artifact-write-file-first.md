<!--
name: Artifact write page to file first
description: >-
  Artifact validation-steer guidance returned to the model, telling it to point
  file_path at the authored .html file and that title only applies when the file
  has no <title> tag
ccVersion: 2.1.239
-->
`file_path` pointing at it (a `title` parameter is used only when the file lacks its own <title> tag).
