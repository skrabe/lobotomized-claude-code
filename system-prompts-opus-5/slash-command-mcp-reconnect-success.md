<!--
name: 'Slash Command: /mcp reconnect — reconnected'
description: >-
  Confirms the named server is connected again, so the model can resume calling
  its tools.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_RECONNECT_SUCCESS_VAR_0
-->
Successfully reconnected to ${SLASH_COMMAND_MCP_RECONNECT_SUCCESS_VAR_0}
