<!--
name: 'Tool Parameter: Artifact Publish Asset Flag'
description: >-
  Artifact tool `asset` flag: with url, upload file_path to the artifact asset
  store instead of publishing the page.
ccVersion: 2.1.257
-->
publish with `url`: true uploads `file_path` to that artifact's asset store instead of publishing it as the page (see **Calls**).
