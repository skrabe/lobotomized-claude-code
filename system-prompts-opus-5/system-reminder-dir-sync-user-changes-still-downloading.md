<!--
name: 'System Reminder: Dir Sync User Changes Still Downloading'
description: >-
  Tells the cloud agent the user's latest changes are still downloading and are
  not in the checkout yet, and to say so if the request depends on them.
ccVersion: 2.1.246
variables:
  - SYSTEM_REMINDER_DIR_SYNC_USER_CHANGES_STILL_DOWNLOADING_VAR_0
-->
Directory sync: the user's latest changes (about ${SYSTEM_REMINDER_DIR_SYNC_USER_CHANGES_STILL_DOWNLOADING_VAR_0} MiB) are still downloading to this session and are NOT in the checkout yet; they will be applied at a later turn. Say so if the user's request depends on them.
