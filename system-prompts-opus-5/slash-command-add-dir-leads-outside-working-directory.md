<!--
name: 'Slash Command: /add-dir — path leads outside the working directory'
description: >-
  Tells the model the directory leads outside the working directory so its
  skills, commands, and agents were not loaded, and that /add-dir would grant
  access.
ccVersion: 2.1.257
variables:
  - SLASH_COMMAND_ADD_DIR_LEADS_OUTSIDE_WORKING_DIRECTORY_VAR_0
-->
${SLASH_COMMAND_ADD_DIR_LEADS_OUTSIDE_WORKING_DIRECTORY_VAR_0} leads outside the working directory, so its skills, commands, and agents weren't loaded. Add that location with /add-dir to grant access to it.
