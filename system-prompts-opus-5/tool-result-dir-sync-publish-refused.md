<!--
name: 'Tool Result: Dir Sync Publish Refused'
description: >-
  go_with_note when the sync service refuses further publishes, so local edits
  are not reaching the host.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_DIR_SYNC_PUBLISH_REFUSED_VAR_0
-->
this session's file sync can no longer publish to ${TOOL_RESULT_DIR_SYNC_PUBLISH_REFUSED_VAR_0} (the sync service refuses it), so your edits here are not reaching that machine; the command ran on its files as they are
