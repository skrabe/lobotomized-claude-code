<!--
name: 'Tool Description: Device Bash Local Sandbox Intro'
description: >-
  Introduces device_bash to the model as a sandboxed shell tool that runs on the
  user's local device rather than in the cloud container.
ccVersion: 2.1.227
-->
Run a shell command on the user's local machine (the device running Claude Code), inside Claude Code's OS sandbox. This is NOT the cloud 
