<!--
name: Asset listing plan-mode approval reason
description: >-
  Decision reason when the first list_assets on an artifact happens in plan
  mode: it admits file names written by someone else into the conversation, so
  approval must come from the user rather than the auto-permission classifier.
ccVersion: 2.1.235
variables:
  - TOOL_RESULT_ARTIFACT_LIST_ASSETS_PLAN_MODE_USER_APPROVAL_REQUIRED_VAR_0
-->
First listing of the assets of ${TOOL_RESULT_ARTIFACT_LIST_ASSETS_PLAN_MODE_USER_APPROVAL_REQUIRED_VAR_0} in plan mode admits file names written by someone else into the conversation — approval must come from the user, not the auto-permission classifier
