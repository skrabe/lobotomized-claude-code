<!--
name: 'project_read: Corrupt Download Retry Notice'
description: >-
  Trailing concatenated half of the `throw Error("project_read: downloaded bytes
  do not match the reported file size …")` raised in k5y(); the dispatcher
  converts the thrown message into an is_error tool_result the model reads.
ccVersion: 2.1.221
-->

concurrently — retry if a save was just issued).
