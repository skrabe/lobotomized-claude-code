<!--
name: 'Slash Command: /resume — session still running as a background agent'
description: >-
  Tells the model the session cannot be resumed here because it is still running
  as a background agent, and where to attach or stop it.
ccVersion: 2.1.233
-->
That session is still running as a background agent. Open `claude agents` to attach to it, or stop it there first to resume here.
