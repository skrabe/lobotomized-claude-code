<!--
name: 'MCP Authenticate: Managed Policy'
description: >-
  MCP authenticate tool call() error result when the server is blocked by
  managed policy; only an org admin can change it.
ccVersion: 2.1.257
variables:
  - TOOL_RESULT_MCP_AUTHENTICATE_MANAGED_POLICY_VAR_0
  - TOOL_RESULT_MCP_AUTHENTICATE_MANAGED_POLICY_VAR_1
-->
${TOOL_RESULT_MCP_AUTHENTICATE_MANAGED_POLICY_VAR_0(TOOL_RESULT_MCP_AUTHENTICATE_MANAGED_POLICY_VAR_1)}. Only an organization admin can change this; do not retry or ask the user to enable it.
