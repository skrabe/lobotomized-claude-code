<!--
name: 'Data: Task notification tail — call resume_replies again only if asked'
description: >-
  Closing instruction of the same watch-ended notification, telling the model to
  inform the user and only re-call resume_replies if they still want
  auto-replies.
ccVersion: 2.1.234
-->
tell the user, and call resume_replies again only if they still want auto-replies resumed
