<!--
name: 'Tool Parameter: Artifact Publish Fallback Title'
description: >-
  Artifact tool title parameter used as fallback when the HTML file has no
  <title>.
ccVersion: 2.1.269
-->
publish: the fallback title for an HTML page whose file has no <title>. It is a name, not a summary, and Claude keeps it the same across redeploys.${TOOL_PARAMETER_ARTIFACT_PUBLISH_FALLBACK_TITLE_VAR_0.typeCreateOn?" On a `type_url` create, it is the new Artifact's name: what the person called it, or a short descriptive name. If it is left out, the Artifact is named after the type.":""}
