<!--
name: Dir Sync Stopped Set Aside Redo On Machine
description: >-
  Stopped-sync suffix pointing at the set-aside copy and telling the model to
  redo unsent changes on the user's machine.
ccVersion: 2.1.251
variables:
  - SYSTEM_REMINDER_DIR_SYNC_STOPPED_SET_ASIDE_REDO_ON_MACHINE_VAR_0
  - SYSTEM_REMINDER_DIR_SYNC_STOPPED_SET_ASIDE_REDO_ON_MACHINE_VAR_1
-->
 — the set-aside copy under ${SYSTEM_REMINDER_DIR_SYNC_STOPPED_SET_ASIDE_REDO_ON_MACHINE_VAR_0(SYSTEM_REMINDER_DIR_SYNC_STOPPED_SET_ASIDE_REDO_ON_MACHINE_VAR_1.setAsideIn)} still holds them; redo those changes on the user's machine if they still matter, and say so
