<!--
name: 'Slash Command: /mcp reconnect — needs authentication, use /mcp'
description: >-
  Sibling of the background-session branch: tells the model the server needs
  authentication and that /mcp is the way to do it, so it stops retrying
  reconnect.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_RECONNECT_NEEDS_AUTH_TERMINAL_VAR_0
-->
${SLASH_COMMAND_MCP_RECONNECT_NEEDS_AUTH_TERMINAL_VAR_0} requires authentication. Use /mcp to authenticate.
