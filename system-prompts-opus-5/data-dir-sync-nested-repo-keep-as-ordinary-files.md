<!--
name: 'Data: Directory sync nested repo keep as ordinary files'
description: >-
  Advice tail interpolated into the nested-repository dir_sync_notice when every
  nested repo can sync if rewritten as ordinary files.
ccVersion: 2.1.246
-->
 — or keep that content as ordinary files (no .git of its own) so it syncs
