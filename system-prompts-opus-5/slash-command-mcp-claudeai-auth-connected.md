<!--
name: 'Slash Command: /mcp — claude.ai auth succeeded and server connected'
description: >-
  Confirms the named MCP server is now authenticated and connected, so the model
  can go ahead and use its tools.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_CLAUDEAI_AUTH_CONNECTED_VAR_0
-->
Authentication successful. Connected to ${SLASH_COMMAND_MCP_CLAUDEAI_AUTH_CONNECTED_VAR_0.name}.
