<!--
name: 'Slash Command: /mcp — server is disabled, enable it first'
description: >-
  Tells the model the named MCP server cannot be reconnected because it is
  disabled and that the fix is `/mcp enable` first, so it retries the right
  action instead of the same one.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_SERVER_DISABLED_ENABLE_FIRST_VAR_0
-->
"${SLASH_COMMAND_MCP_SERVER_DISABLED_ENABLE_FIRST_VAR_0}" is disabled — enable it in /mcp first
