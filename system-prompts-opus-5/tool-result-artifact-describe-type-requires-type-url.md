<!--
name: Artifact describe_type Requires type_url
description: validateInput rejection when describe_type is called without type_url.
ccVersion: 2.1.246
-->
action "describe_type" requires `type_url` — the Artifact type's link, from a list_types result.
