<!--
name: Artifact read_file save — protected name inside scratchpad
description: >-
  Reason fragment (Nn) in the Artifact tool's checkPermissions read/read_file
  branch; feeds the deny message `${Nn}, and no one can answer the prompt in
  this session — ${Mr}` returned as behavior:"deny", which the model reads as
  the tool_result.
ccVersion: 2.1.239
-->
this published path carries a name the file-edit safety rules screen even inside the scratchpad (a git or bare-repository layout, hook, tool or agent configuration directories), so saving it needs the user’s approval
