<!--
name: 'Slash Command: /mcp — OAuth succeeded and server reconnected'
description: >-
  Confirms the OAuth flow completed and a previously authenticated server is
  connected again, so the model can use its tools.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_MCP_OAUTH_RECONNECTED_VAR_0
-->
Authentication successful. Reconnected to ${SLASH_COMMAND_MCP_OAUTH_RECONNECTED_VAR_0.name}.
