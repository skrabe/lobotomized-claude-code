<!--
name: Artifact MCP connector guidance — claude.ai connectors plus the meta connector
description: >-
  Variant of the valid-`server`-values clause used when a meta connector is
  present, naming it alongside the user's claude.ai connectors and excluding the
  session's other built-in MCP servers.
ccVersion: 2.1.239
variables:
  - DATA_ARTIFACT_MCP_CONNECTOR_VALID_SERVERS_CLAUDE_AI_PLUS_META_VAR_0
-->
 Only connectors the user added in claude.ai and \`${DATA_ARTIFACT_MCP_CONNECTOR_VALID_SERVERS_CLAUDE_AI_PLUS_META_VAR_0.server}\` are valid \`server\` values — this session's other built-in MCP servers are not.
