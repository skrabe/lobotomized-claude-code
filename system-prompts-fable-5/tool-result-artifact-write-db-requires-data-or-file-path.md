<!--
name: 'Tool Result: Artifact write_db Requires data or file_path'
description: >-
  validateInput rejection when write_db set/update is missing both `data` and
  `file_path`.
ccVersion: 2.1.237
variables:
  - TOOL_RESULT_ARTIFACT_WRITE_DB_REQUIRES_DATA_OR_FILE_PATH_VAR_0
-->
db_op "${TOOL_RESULT_ARTIFACT_WRITE_DB_REQUIRES_DATA_OR_FILE_PATH_VAR_0}" requires the document — pass \`data\` (inline object) or \`file_path\` (a local JSON file).
