<!--
name: 'Data: Import untranslatable nested brace path'
description: >-
  Unmappable-item reason for an @{…} path with a nested brace the importer would
  truncate, interpolated into the /import query prompt and the
  import-to-claude-code SKILL.md
ccVersion: 2.1.214
-->
Its `@{…}` path contains a nested brace, which this importer would truncate.
