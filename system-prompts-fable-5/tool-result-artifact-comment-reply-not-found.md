<!--
name: 'Artifact Comment Reply: Thread Not Found'
description: >-
  HTTP-404 branch of the `{kind:"error",message}` returned by sap(); the
  Artifact tool's reply action throws `new Sp(Mt.message,
  `reply_${Mt.reason}`)`, delivering it to the model as a tool error.
ccVersion: 2.1.221
-->

artifact or comment thread not found — it may have been deleted, the artifact republished, or you may not have access
