<!--
name: Artifact tool description — To delete section
description: >-
  The **To delete** section of the Artifact tool description explaining how and
  when to delete a published Artifact.
ccVersion: 2.1.239
-->
**To delete**: if the user says they did not want something published, or no longer wants an Artifact online, pass `action: "delete"` with its `url`. Afterwards, give them the content the way they wanted it (for example, as the local file).
