<!--
name: Artifact Publish Comment Fields Invalid
description: >-
  Tool validation result telling the model that thread_id and text are only
  valid for the Artifact comments and reply actions.
ccVersion: 2.1.227
-->
`thread_id` applies only to actions "comments" and "reply", and `text` only to "reply"
