<!--
name: 'Data: Agent Import — Cursor Settings Rules'
description: >-
  Hardcoded unmappable `reason` for Cursor Settings → Rules (not on disk);
  interpolated into the /import summary prompt.
ccVersion: 2.1.265
-->
Stored in Cursor Settings → Rules, not on disk. Paste the ones you want into ~/.claude/CLAUDE.md (your user-level memory file).
