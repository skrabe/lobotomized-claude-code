<!--
name: 'Data: footerLinksRegexes.(regex).pattern setting description'
description: >-
  Description of the `footerLinksRegexes.(regex).pattern` setting in Claude
  Code's settings JSON schema. The model reads it through /update-config and
  settings validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Regex matched against turn output (tool results and assistant text)
