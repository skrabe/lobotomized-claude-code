<!--
name: 'Data: sandbox.credentials.injectHosts setting description (part 1 of 3)'
description: >-
  Description of the `sandbox.credentials.injectHosts` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Optional narrowing of where the proxy substitutes this credential. Only meaningful when mode is `mask`; accepted but ignored for `deny`. If unset, defaults to 
