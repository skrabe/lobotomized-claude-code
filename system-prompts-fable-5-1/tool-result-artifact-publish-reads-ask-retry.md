<!--
name: 'Tool Result: Artifact Publish Reads Need Approval'
description: >-
  Thrown to the model when publishing is blocked because a Read permission rule
  asked rather than allowed, telling it to retry so the read can be approved.
ccVersion: 2.1.238
-->
Publishing reads file contents and a Read permission check requires approval here — retry the publish so it can be asked.
