<!--
name: >-
  Data: extraKnownMarketplaces.source.(settings).plugins.source.(npm).registry
  setting description
description: >-
  Description of the
  `extraKnownMarketplaces.source.(settings).plugins.source.(npm).registry`
  setting in Claude Code's settings JSON schema. The model reads it through
  /update-config and settings validation errors; it is also shown to users in
  the settings help.
ccVersion: 2.1.276
-->
Custom NPM registry URL (defaults to using system default, likely npmjs.org)
