<!--
name: Artifact write_db Someone Else's Asks Until Read
description: >-
  write_db description() clause when editing someone else's artifact before this
  conversation has read its data, so approval covers only that call.
ccVersion: 2.1.265
-->
an edit inside someone else's artifact asks on its own, and approving covers only it, until this conversation has read that artifact's data
