<!--
name: 'Tool result: SendFile cross-machine unavailable'
description: >-
  SendFile tool-result returned to the model when cross-machine transfer is
  blocked by the provider/privacy configuration, noting same-machine transfers
  still work.
ccVersion: 2.1.210
-->
Cross-machine file transfer is unavailable: it uploads file contents through Anthropic servers, which this provider/privacy configuration does not allow. Same-machine (uds:) transfers still work.
