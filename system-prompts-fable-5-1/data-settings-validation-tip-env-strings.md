<!--
name: 'Data: settings validation tip (env value not a string)'
description: >-
  Suggestion appended to a settings.json validation error (env value not a
  string); the model reads it in the tool result when its edit to a settings
  file fails validation.
ccVersion: 2.1.276
-->
Environment variables must be strings. Wrap numbers and booleans in quotes. Example: "DEBUG": "true", "PORT": "3000"
