<!--
name: 'Data: sshConfigs.id setting description'
description: >-
  Description of the `sshConfigs.id` setting in Claude Code's settings JSON
  schema. The model reads it through /update-config and settings validation
  errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Unique identifier for this SSH config. Used to match configs across settings sources.
