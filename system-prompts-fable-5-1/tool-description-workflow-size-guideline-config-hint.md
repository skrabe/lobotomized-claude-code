<!--
name: Workflow Size Guideline Config Hint
description: >-
  WIPED — a config-discoverability CTA about a user-side /config row, not an
  instruction the model acts on. The advisory-not-a-hard-limit claim is carried
  by the deployed tool-description-workflow-size-guideline-note. Trailing
  sentence appended to the Workflow tool description when the size guideline is
  the default, telling the model the user can change it via /config.
ccVersion: 2.1.219
-->

