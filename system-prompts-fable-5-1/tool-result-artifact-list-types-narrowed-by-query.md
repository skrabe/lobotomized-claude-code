<!--
name: 'Tool Result: Artifact list types narrowed by query'
description: >-
  Notes that an Artifact type listing only shows types matching `type_query`, so
  a type may be missing, and to list again without it to see all
ccVersion: 2.1.277
-->

This listing shows only the types that match the `type_query` text, so a type this account has may be missing from it. To list without narrowing, call the listing again without `type_query`.
