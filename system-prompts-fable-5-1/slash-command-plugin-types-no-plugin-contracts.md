<!--
name: 'Slash Command: /plugin-types Wrote No Plugin Contracts'
description: >-
  /plugin-types success line when no enabled plugin names a type contract in
  plugin.json types, so the generated index declares nothing.
ccVersion: 2.1.273
variables:
  - SLASH_COMMAND_PLUGIN_TYPES_NO_PLUGIN_CONTRACTS_VAR_0
-->
Wrote ${SLASH_COMMAND_PLUGIN_TYPES_NO_PLUGIN_CONTRACTS_VAR_0}: no enabled plugin names a type contract (plugin.json \`types\`), so it declares nothing.
