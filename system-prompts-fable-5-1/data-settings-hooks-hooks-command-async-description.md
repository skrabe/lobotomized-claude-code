<!--
name: 'Data: hooks.hooks.(command).async setting description'
description: >-
  Description of the `hooks.hooks.(command).async` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
If true, hook runs in background without blocking
