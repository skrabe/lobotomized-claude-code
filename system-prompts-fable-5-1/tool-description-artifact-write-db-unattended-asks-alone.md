<!--
name: Artifact write_db Unattended Asks Alone
description: >-
  write_db description() clause when the edit follows an unattended auto-reply
  notification, so approval covers only that call.
ccVersion: 2.1.265
-->
requested after an unattended auto-reply notification, so this edit asks on its own and approving covers only it
