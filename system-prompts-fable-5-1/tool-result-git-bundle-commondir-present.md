<!--
name: 'Tool Result: Git Bundle Commondir Present'
description: >-
  Previous-way clause refusing the upload when the git directory holds a
  commondir file git writes only for a linked worktree.
ccVersion: 2.1.280
-->
its git directory holds a commondir file, which git writes only in a linked working tree’s entry and which would send that capture to another repository’s configuration — remove it, or start from an ordinary clone
