<!--
name: 'Tool Result: Artifact DB Cursor Unreadable'
description: >-
  Trailer on the artifact read_db tool_result telling the model more documents
  exist but the continuation cursor was malformed, so the read must be re-run.
ccVersion: 2.1.224
-->

[more documents exist, but the continuation cursor was unreadable — re-run the read]
