<!--
name: SendFile Stale Bridge ID Dynamic Hint
description: >-
  SendFile failure suffix telling the model that an explicit bridge ID is stale
  after a peer restart and optionally appending a current-session listing hint.
ccVersion: 2.1.226
variables:
  - TOOL_RESULT_SENDFILE_BRIDGE_ID_STALE_DYNAMIC_HINT_VAR_0
-->
 — the peer session may have ended or restarted, so this bridge ID is stale.${TOOL_RESULT_SENDFILE_BRIDGE_ID_STALE_DYNAMIC_HINT_VAR_0}
