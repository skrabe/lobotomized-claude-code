<!--
name: 'Slash Command: /model — effort is session-only'
description: >-
  Parenthetical appended to the /model confirmation when an effort level came
  from ultracode but ultracode is off, telling the model that effort remains
  session-scoped even if the model was saved as the default.
ccVersion: 2.1.246
-->
 (the effort applies to this session only)
