<!--
name: 'Tool Result: Git Bundle Git File Symlink'
description: >-
  Previous-way clause refusing the upload when .git is a symlink the checkout's
  own session can retarget.
ccVersion: 2.1.280
-->
its .git entry is a symbolic link, which this checkout’s own session can re-point and nothing pins in place, so where it leads when that capture runs cannot be bound to this tree — start from an ordinary clone instead
