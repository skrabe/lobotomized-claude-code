<!--
name: 'Data: settings validation tip (permissions.defaultMode invalid value)'
description: >-
  Suggestion appended to a settings.json validation error
  (permissions.defaultMode invalid value); the model reads it in the tool result
  when its edit to a settings file fails validation.
ccVersion: 2.1.276
-->
Valid modes: "acceptEdits" (ask before file changes), "plan" (analysis only), "bypassPermissions" (auto-accept all), or "default" (standard behavior)
