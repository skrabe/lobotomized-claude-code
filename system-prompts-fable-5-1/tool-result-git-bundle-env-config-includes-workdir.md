<!--
name: 'Tool Result: Git Bundle Env Config Includes Workdir'
description: >-
  pC includes-clause for GIT_CONFIG_PARAMETERS when that environment config
  includes a file inside the working tree.
ccVersion: 2.1.280
variables:
  - TOOL_RESULT_GIT_BUNDLE_ENV_CONFIG_INCLUDES_WORKDIR_VAR_0
-->
its launch environment’s git configuration (GIT_CONFIG_PARAMETERS / GIT_CONFIG_COUNT) includes a file inside this working tree or its repository — configuration this upload could not have git list here, where a file a session may write could say what that capture runs; ${TOOL_RESULT_GIT_BUNDLE_ENV_CONFIG_INCLUDES_WORKDIR_VAR_0}drop that include from the environment, or start from an ordinary clone
