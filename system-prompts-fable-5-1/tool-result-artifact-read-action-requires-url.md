<!--
name: Artifact read action requires url
description: >-
  Validation error returned to the model when action "read" is called without
  `url`.
ccVersion: 2.1.239
-->
action "read" requires `url` — the artifact's claude.ai URL (find it with action: "list").
