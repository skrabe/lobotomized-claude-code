<!--
name: Artifact publish base is a symlink — user approval required
description: >-
  safetyCheck decisionReason on the publish ask when the publish base resolves
  through a symlink to a different directory.
ccVersion: 2.1.274
variables:
  - TOOL_RESULT_ARTIFACT_PUBLISH_SYMLINK_BASE_USER_APPROVAL_REQUIRED_VAR_0
  - TOOL_RESULT_ARTIFACT_PUBLISH_SYMLINK_BASE_USER_APPROVAL_REQUIRED_VAR_1
-->
The publish base is a symlink to a different directory — approval must see the canonical target, which ${TOOL_RESULT_ARTIFACT_PUBLISH_SYMLINK_BASE_USER_APPROVAL_REQUIRED_VAR_0}${TOOL_RESULT_ARTIFACT_PUBLISH_SYMLINK_BASE_USER_APPROVAL_REQUIRED_VAR_1}
