<!--
name: PushNotification skipped (user present)
description: >-
  Tool result telling the model the push was not sent because the terminal is
  active.
ccVersion: 2.1.206
-->
Not sent — this terminal is active, so your output here already reaches the user; a separate notification would be redundant.
