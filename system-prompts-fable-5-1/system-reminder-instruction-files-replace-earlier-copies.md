<!--
name: 'System Reminder: Instruction files replace earlier copies'
description: >-
  Preamble joining re-read instruction files, stating each copy replaces the
  earlier one and the instructions header still applies.
ccVersion: 2.1.252
variables:
  - SYSTEM_REMINDER_INSTRUCTION_FILES_REPLACE_EARLIER_COPIES_VAR_0
  - SYSTEM_REMINDER_INSTRUCTION_FILES_REPLACE_EARLIER_COPIES_VAR_1
-->
${SYSTEM_REMINDER_INSTRUCTION_FILES_REPLACE_EARLIER_COPIES_VAR_0} — each replaces its earlier copy, and the instructions preamble above still applies:

${SYSTEM_REMINDER_INSTRUCTION_FILES_REPLACE_EARLIER_COPIES_VAR_1}
