<!--
name: Artifact write_db Version-Pinned Someone Else's Asks Until Read
description: >-
  write_db description() clause when a version-pinned write targets someone
  else's artifact before this conversation has read its data.
ccVersion: 2.1.267
-->
a version-pinned write to someone else's artifact asks on its own, and approving covers only it, until this conversation has read that artifact's data
