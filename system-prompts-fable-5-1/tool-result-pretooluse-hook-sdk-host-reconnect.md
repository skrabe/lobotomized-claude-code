<!--
name: Pretooluse Hook Sdk Host Reconnect
description: >-
  Synthetic PreToolUse deny reason when the SDK host reconnects before the hook
  answers, returned to the model as the blocked tool result.
ccVersion: 2.1.238
-->
The SDK host reconnected before its PreToolUse hook answered, so this tool call was not executed. No one denied it; retry the same tool call.
