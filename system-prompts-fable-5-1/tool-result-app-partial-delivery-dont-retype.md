<!--
name: 'Tool Result: app_type partial delivery do not retype'
description: >-
  Suffix spliced into isError app_* tool_results when a type was interrupted
  after partial delivery, forbidding a full retype.
ccVersion: 2.1.246
-->
fresh app_screenshot and do NOT re-type the whole string (that would append on top of the prefix). Type only the remainder, or clear the field first.
