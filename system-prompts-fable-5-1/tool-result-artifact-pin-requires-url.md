<!--
name: Artifact Pin Requires URL
description: >-
  Pin/unpin validateInput failure (errorCode 7) telling the model action
  pin/unpin requires the artifact's claude.ai url.
ccVersion: 2.1.259
variables:
  - TOOL_RESULT_ARTIFACT_PIN_REQUIRES_URL_VAR_0
-->
action "${TOOL_RESULT_ARTIFACT_PIN_REQUIRES_URL_VAR_0}" requires \`url\` — the artifact's claude.ai URL (the publish result has it; action: "list" shows earlier ones).
