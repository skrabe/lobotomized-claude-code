<!--
name: 'Tool Result: Artifact Session Gateway Transient HTTP Retry'
description: >-
  Artifact-read tool_result lead when the session gateway could not serve the
  read after one retry, telling the model the failure is transient.
ccVersion: 2.1.273
variables:
  - TOOL_RESULT_ARTIFACT_SESSION_GATEWAY_TRANSIENT_HTTP_RETRY_VAR_0
-->
the session gateway could not serve this read just now (HTTP ${TOOL_RESULT_ARTIFACT_SESSION_GATEWAY_TRANSIENT_HTTP_RETRY_VAR_0}, after one retry); this is transient — retry shortly
