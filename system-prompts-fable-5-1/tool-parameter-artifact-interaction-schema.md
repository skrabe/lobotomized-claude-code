<!--
name: 'Tool Parameter: Artifact `schema`'
description: >-
  Description of the Artifact tool's optional `schema` input parameter naming
  which registered interaction schema to validate a page's data island against;
  shipped to the model inside the tool's input_schema.
ccVersion: 2.1.219
variables:
  - TOOL_PARAMETER_ARTIFACT_INTERACTION_SCHEMA_VAR_0
-->
Required for read_page_data (e.g. "${TOOL_PARAMETER_ARTIFACT_INTERACTION_SCHEMA_VAR_0[0]}"); meaningless for every other action.
