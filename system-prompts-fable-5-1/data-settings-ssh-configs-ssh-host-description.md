<!--
name: 'Data: sshConfigs.sshHost setting description'
description: >-
  Description of the `sshConfigs.sshHost` setting in Claude Code's settings JSON
  schema. The model reads it through /update-config and settings validation
  errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
SSH host in format "user@hostname" or "hostname", or a host alias from ~/.ssh/config
