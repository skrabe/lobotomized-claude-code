<!--
name: Bash Sandbox Permission-Gate Note
description: >-
  Sandbox-guidance sub-bullet in the Bash tool description explaining that a
  dangerouslyDisableSandbox retry passes through the permission gate; injected
  into the model's Bash tool schema.
ccVersion: 2.1.207
-->
This goes through the permission gate (a user prompt, or the auto-mode classifier when auto mode is active)
