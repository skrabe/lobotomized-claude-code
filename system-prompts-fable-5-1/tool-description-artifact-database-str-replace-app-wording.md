<!--
name: 'Tool Description: Artifact Database str_replace (App Wording)'
description: >-
  Person-worded database-description splice for in-place str_replace without
  resending the field.
ccVersion: 2.1.273
-->
 "str_replace" swaps one exact, unique `old_str` for `new_str` inside a string `field` without resending it (`replace_all: true` for every occurrence),
