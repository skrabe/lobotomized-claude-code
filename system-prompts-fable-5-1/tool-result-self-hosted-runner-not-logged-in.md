<!--
name: 'Tool Result: Self-hosted runner not logged in'
description: >-
  Error thrown by the self-hosted-runner API auth helper; reaches the model as
  <tool_use_error> content telling it to run `claude login` because
  ANTHROPIC_API_KEY will not work.
ccVersion: 2.1.224
-->
Not logged in. Environment-management endpoints require the operator OAuth session — run `claude login`, then re-invoke this tool. (`ANTHROPIC_API_KEY` will not work for these endpoints.)
