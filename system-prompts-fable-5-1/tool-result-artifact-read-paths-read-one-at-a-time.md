<!--
name: 'Tool Result: Artifact read — read paths one at a time'
description: >-
  Remedy clause on the artifact read_file approval result, naming the `path`
  parameter the model must fall back to when a batched save is not approved.
ccVersion: 2.1.273
-->
read them one at a time with `path` instead
