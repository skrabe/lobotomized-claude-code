<!--
name: 'Data: /install-github-app needs an attached terminal'
description: >-
  Status message added to the conversation when /install-github-app runs in a
  background session with no terminal attached.
ccVersion: 2.1.218
-->
Can't run /install-github-app while no terminal is attached to this background session. This session now shows "needs input" in agent view — open it and run the command again.
