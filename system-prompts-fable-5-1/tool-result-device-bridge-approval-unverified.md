<!--
name: Device Bridge Approval Unverified
description: >-
  Tool result when this session could not verify the user's approval, so the
  remote call did not run.
ccVersion: 2.1.247
variables:
  - TOOL_RESULT_DEVICE_BRIDGE_APPROVAL_UNVERIFIED_VAR_0
-->
The approval for this call could not be verified as the user's own answer, so the call was not cleared to run on ${TOOL_RESULT_DEVICE_BRIDGE_APPROVAL_UNVERIFIED_VAR_0} and nothing ran. Ask again and have the user approve from the terminal or desktop prompt, unedited.
