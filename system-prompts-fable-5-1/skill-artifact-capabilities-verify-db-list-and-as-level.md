<!--
name: 'Skill: Artifact Capabilities Verify Database List And As Level'
description: >-
  Fragment of the artifact-capabilities skill prompt telling the model, after
  the first publish, to list each written collection and repeat the read at a
  lower as_level.
ccVersion: 2.1.280
variables:
  - SKILL_ARTIFACT_CAPABILITIES_VERIFY_DB_LIST_AND_AS_LEVEL_VAR_0
-->
after the first publish, one \`${SKILL_ARTIFACT_CAPABILITIES_VERIFY_DB_LIST_AND_AS_LEVEL_VAR_0}\` \`list\` of each collection the page writes, and, where its rules hide something from ordinary viewers, the same read with a lower \`as_level\`, which must not show what the rules hide from such a viewer
