<!--
name: 'Remote Tool: Late Ask After Approval'
description: >-
  Late refusal when a session ask-rule appears after the call was already
  approved, so the host is not contacted.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_REMOTE_TOOL_LATE_ASK_AFTER_APPROVAL_VAR_0
  - TOOL_RESULT_REMOTE_TOOL_LATE_ASK_AFTER_APPROVAL_VAR_1
-->
A permission rule of this session now asks before this ${TOOL_RESULT_REMOTE_TOOL_LATE_ASK_AFTER_APPROVAL_VAR_0.name} call; it appeared after the approval. ${TOOL_RESULT_REMOTE_TOOL_LATE_ASK_AFTER_APPROVAL_VAR_1.name} was not contacted.
