<!--
name: Reads Outside Working Directories Blocked
description: >-
  Permission ask/deny reason when blockReadsOutsideWorkingDirectories blocks a
  path outside the working directories.
ccVersion: 2.1.257
-->
Reads outside the working directories are blocked (permissions.blockReadsOutsideWorkingDirectories). Add the directory with /add-dir, or remove that setting.
