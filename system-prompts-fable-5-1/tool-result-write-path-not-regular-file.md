<!--
name: 'Tool Result: Write Path Not A Regular File'
description: >-
  Write validateInput error returned to the model when file_path exists but is a
  device, FIFO or socket rather than a regular file.
ccVersion: 2.1.277
variables:
  - TOOL_RESULT_WRITE_PATH_NOT_REGULAR_FILE_VAR_0
-->
${TOOL_RESULT_WRITE_PATH_NOT_REGULAR_FILE_VAR_0} exists but is not a regular file (a device, FIFO or socket). Write only creates or overwrites regular files.
