<!--
name: 'Artifact conflict — read-only access, cannot publish'
description: >-
  Conflict explanation for the no_write_access case: the session can only view
  this artifact and can never publish to it.
ccVersion: 2.1.239
-->
This session has read-only access to this artifact (it was shared for viewing, or it is someone else's public artifact), so it can never publish to it: tell the user; to keep your version, publish it as a new artifact of their own instead.
