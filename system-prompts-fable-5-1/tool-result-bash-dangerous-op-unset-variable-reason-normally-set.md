<!--
name: 'Tool Result: Bash Dangerous Op Reason — Normally-Set Variable'
description: >-
  Short remedy clause in the dangerous-removal decisionReason when the target's
  variable is normally set, so a guard would not stop it; suggests a literal
  path.
ccVersion: 2.1.277
variables:
  - TOOL_RESULT_BASH_DANGEROUS_OP_UNSET_VARIABLE_REASON_NORMALLY_SET_VAR_0
-->
use a literal path: ${TOOL_RESULT_BASH_DANGEROUS_OP_UNSET_VARIABLE_REASON_NORMALLY_SET_VAR_0} is normally set, so a guard would not stop this
