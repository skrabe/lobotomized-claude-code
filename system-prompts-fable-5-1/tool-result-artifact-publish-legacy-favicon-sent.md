<!--
name: Artifact Publish Legacy Favicon Sent
description: >-
  Publish tool_result note that a legacy favicon emoji was sent and the next
  publish should pass icon instead.
ccVersion: 2.1.276
-->
 Icon: the legacy `favicon` emoji was sent (next time pass `icon`, one generic word, instead).
