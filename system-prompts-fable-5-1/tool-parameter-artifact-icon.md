<!--
name: 'Tool Parameter: Artifact Icon'
description: >-
  Zod .describe() for the Artifact tool icon parameter: a short generic word for
  the artifact's tab icon.
ccVersion: 2.1.276
variables:
  - TOOL_PARAMETER_ARTIFACT_ICON_VAR_0
-->
One short generic word for the artifact's browser-tab icon, such as chart, calendar, recipe, code or map — a plain signifier, not a product or brand name. Include it on every page's first publish; omit when republishing to keep the current icon, and pass a new one only when the user asks.${TOOL_PARAMETER_ARTIFACT_ICON_VAR_0?" Ignored on an Artifact created from an Artifact type.":""}
