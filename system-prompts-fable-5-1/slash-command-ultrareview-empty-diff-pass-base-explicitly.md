<!--
name: 'Slash Command: /ultrareview empty diff — pass a base explicitly'
description: >-
  Remedy clause appended to the /ultrareview empty-diff hints when no base was
  passed: pass one explicitly, with the exact command form `<cmd> <branch>`.
ccVersion: 2.1.277
variables:
  - SLASH_COMMAND_ULTRAREVIEW_EMPTY_DIFF_PASS_BASE_EXPLICITLY_VAR_0
-->
pass one explicitly, e.g. \`${SLASH_COMMAND_ULTRAREVIEW_EMPTY_DIFF_PASS_BASE_EXPLICITLY_VAR_0} <branch>\`
