<!--
name: 'Tool Result: Worktree git guard — unmodeled env wrapper'
description: >-
  Refusal clause: an env invocation carries flags whose effect on the command it
  wraps can't be verified.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_ENV_UNMODELED_VAR_0
-->
runs env with ${TOOL_RESULT_BASH_WORKTREE_GIT_ENV_UNMODELED_VAR_0.envUnmodeled}, whose effect on the command it wraps can't be verified
