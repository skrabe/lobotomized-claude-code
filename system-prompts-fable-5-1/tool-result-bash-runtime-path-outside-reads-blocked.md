<!--
name: Bash Runtime Path Outside Reads Blocked
description: >-
  checkPermissions ask message when a bash path is computed at runtime and
  cannot be checked against the read block.
ccVersion: 2.1.257
variables:
  - TOOL_RESULT_BASH_RUNTIME_PATH_OUTSIDE_READS_BLOCKED_VAR_0
-->
${TOOL_RESULT_BASH_RUNTIME_PATH_OUTSIDE_READS_BLOCKED_VAR_0} names a path that is computed at run time, which cannot be checked against the read block (permissions.blockReadsOutsideWorkingDirectories)
