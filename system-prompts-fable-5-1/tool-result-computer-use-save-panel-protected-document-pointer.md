<!--
name: Computer-use save-panel protected-document pointer block
description: >-
  Tool-result blocking pointer input into a window that is editing a protected
  shell-startup, ssh, or launch-agent file.
ccVersion: 2.1.246
-->
Pointer input into this window is not allowed: it is editing a protected file (a shell-startup, ssh, or launch-agent path). Close it with cmd+w, click a different app's window, or ask the user to close it.
