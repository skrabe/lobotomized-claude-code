<!--
name: 'Slash Command: /web-setup — GitHub CLI not authenticated'
description: >-
  Tells the model `gh` is installed but has no credentials, and names the two
  ways to fix it before /web-setup can succeed.
ccVersion: 2.1.233
variables:
  - SLASH_COMMAND_WEB_SETUP_GH_CLI_NOT_AUTHENTICATED_VAR_0
-->
GitHub CLI not authenticated. Run \`gh auth login\` and try again, or connect GitHub on the web: ${SLASH_COMMAND_WEB_SETUP_GH_CLI_NOT_AUTHENTICATED_VAR_0}
