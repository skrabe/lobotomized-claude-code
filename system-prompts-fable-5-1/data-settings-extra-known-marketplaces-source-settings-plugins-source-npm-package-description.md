<!--
name: >-
  Data: extraKnownMarketplaces.source.(settings).plugins.source.(npm).package
  setting description
description: >-
  Description of the
  `extraKnownMarketplaces.source.(settings).plugins.source.(npm).package`
  setting in Claude Code's settings JSON schema. The model reads it through
  /update-config and settings validation errors; it is also shown to users in
  the settings help.
ccVersion: 2.1.276
-->
Package name (or url, or local path, or anything else that can be passed to `npm` as a package)
