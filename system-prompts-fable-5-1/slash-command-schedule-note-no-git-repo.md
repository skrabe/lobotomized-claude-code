<!--
name: '/schedule setup note: no git repo'
description: >-
  Setup note pushed into the /schedule prompt's setupNotes and rendered by vmb()
  into the command text sent to the model.
ccVersion: 2.1.206
-->
Not in a git repo — you'll need to specify a repo URL manually (or skip repos entirely).
