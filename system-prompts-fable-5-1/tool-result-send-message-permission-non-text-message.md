<!--
name: Permission handler rewrote message to non-text
description: >-
  Result returned to Claude when the permission handler turned the peer-send
  message into a structured non-text form that cannot be sent, so nothing was
  sent.
ccVersion: 2.1.234
-->
The permission handler rewrote the message into a structured (non-text) form, which cannot be sent to another session — nothing was sent.
