<!--
name: 'Tool Result: Git Bundle Admin Dir Cloud Writable Placement'
description: >-
  Git-bundle placement refusal when the private git directory would sit where a
  cloud session can write.
ccVersion: 2.1.247
variables:
  - TOOL_RESULT_GIT_BUNDLE_ADMIN_DIR_CLOUD_WRITABLE_PLACEMENT_VAR_0
-->
Not uploading this working tree this way: the private git directory the upload must trust would stand where a cloud session can write — ${TOOL_RESULT_GIT_BUNDLE_ADMIN_DIR_CLOUD_WRITABLE_PLACEMENT_VAR_0}. Nothing was uploaded; retry once that is changed.
