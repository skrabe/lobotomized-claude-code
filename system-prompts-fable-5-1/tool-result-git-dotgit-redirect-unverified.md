<!--
name: .git redirect cannot be verified safe
description: >-
  Command-safety approval reason surfaced to the model when a .git file/symlink
  redirects to an unverifiable location.
ccVersion: 2.1.206
-->
The .git file or symlink here redirects to a location that cannot be verified as safe (it may have been planted by an untrusted archive). Git commands need approval.
