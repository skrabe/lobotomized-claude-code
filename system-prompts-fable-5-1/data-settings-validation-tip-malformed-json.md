<!--
name: 'Data: settings validation tip (malformed JSON)'
description: >-
  Suggestion appended to a settings.json validation error (malformed JSON); the
  model reads it in the tool result when its edit to a settings file fails
  validation.
ccVersion: 2.1.276
-->
Check for missing commas, unmatched brackets, or trailing commas. Use a JSON validator to identify the exact syntax error.
