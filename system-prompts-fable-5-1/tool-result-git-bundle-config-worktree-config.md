<!--
name: 'Tool Result: Git Bundle Config Worktree Config'
description: pC clause when a named git config file turns on extensions.worktreeConfig.
ccVersion: 2.1.280
variables:
  - TOOL_RESULT_GIT_BUNDLE_CONFIG_WORKTREE_CONFIG_VAR_0
  - TOOL_RESULT_GIT_BUNDLE_CONFIG_WORKTREE_CONFIG_VAR_1
-->
its git configuration (${TOOL_RESULT_GIT_BUNDLE_CONFIG_WORKTREE_CONFIG_VAR_0}) switches per-worktree configuration on (extensions.worktreeConfig), under which that capture would also read a configuration file a session of the repository can plant in this tree’s administrative entry; ${TOOL_RESULT_GIT_BUNDLE_CONFIG_WORKTREE_CONFIG_VAR_1}start from the repository’s main checkout
