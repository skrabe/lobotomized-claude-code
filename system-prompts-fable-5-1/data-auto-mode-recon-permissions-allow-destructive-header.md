<!--
name: 'Recon: destructive allow entries header'
description: >-
  Section header (ips) in the auto-mode recon "Existing auto-mode settings"
  block listing destructive permissions.allow entries honored at runtime; part
  of the model-facing recon context.
ccVersion: 2.1.210
-->
#### Destructive permissions.allow entries (honored at runtime — auto-approved with no prompt, in your user settings)
