<!--
name: 'Data: Agent Import — Headless Scan Intro'
description: >-
  Opening line of the `/import` query prompt (BA_), delivered to the model as an
  isMeta user message when the surface has no interactive picker.
ccVersion: 2.1.214
variables:
  - DATA_AGENT_IMPORT_HEADLESS_SCAN_INTRO_VAR_0
-->
The user ran \`/import\` on a surface without the interactive picker. A scan for ${DATA_AGENT_IMPORT_HEADLESS_SCAN_INTRO_VAR_0} config found the following.
