<!--
name: 'Data: hooks.hooks.(http).url setting description'
description: >-
  Description of the `hooks.hooks.(http).url` setting in Claude Code's settings
  JSON schema. The model reads it through /update-config and settings validation
  errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
URL to POST the hook input JSON to
