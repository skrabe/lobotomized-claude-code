<!--
name: 'Tool Hosts Notice: Say Where Separate Copy'
description: >-
  Coordinator instruction that project work belongs in this checkout, and a
  worker prompt should name the host when the task is about the user's separate
  copy.
ccVersion: 2.1.280
variables:
  - SYSTEM_REMINDER_TOOL_HOSTS_SAY_WHERE_SEPARATE_VAR_0
-->
project work belongs here, and when a task is about the user's copy, or about something that exists only on ${SYSTEM_REMINDER_TOOL_HOSTS_SAY_WHERE_SEPARATE_VAR_0}, say in the prompt you give the worker that it runs on ${SYSTEM_REMINDER_TOOL_HOSTS_SAY_WHERE_SEPARATE_VAR_0}, where it acts on that copy only
