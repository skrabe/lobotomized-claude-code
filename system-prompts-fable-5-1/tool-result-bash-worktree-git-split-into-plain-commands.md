<!--
name: 'Tool Result: Worktree Git Split Into Plain Commands'
description: >-
  Advice tail passed as y to i() when the command is too complex to verify,
  returned as bash stderr via Mft.
ccVersion: 2.1.238
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_SPLIT_INTO_PLAIN_COMMANDS_VAR_0
-->
Split it into plain, separate commands and run them from ${TOOL_RESULT_BASH_WORKTREE_GIT_SPLIT_INTO_PLAIN_COMMANDS_VAR_0}.
