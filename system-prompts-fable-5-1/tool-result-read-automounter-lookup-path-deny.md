<!--
name: 'Tool Result: Read Automounter Lookup Path Deny'
description: >-
  Permission deny message when a read path is a darwin automounter lookup whose
  resolution would itself be a network request.
ccVersion: 2.1.274
variables:
  - TOOL_RESULT_READ_AUTOMOUNTER_LOOKUP_PATH_DENY_VAR_0
-->
${TOOL_RESULT_READ_AUTOMOUNTER_LOOKUP_PATH_DENY_VAR_0} names an automounter lookup path (its lookup is itself a network request); it was not read.
