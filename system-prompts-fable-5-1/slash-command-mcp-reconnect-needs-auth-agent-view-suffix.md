<!--
name: 'Slash Command: MCP reconnect needs-auth agent-view suffix'
description: >-
  Suffix appended to the /mcp reconnect needs-auth result noting the session now
  shows 'needs input' in the agent view.
ccVersion: 2.1.218
-->
 It now shows "needs input" in agent view.
