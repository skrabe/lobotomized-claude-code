<!--
name: Path contains .. traversal after a segment
description: >-
  Command-safety reason surfaced to the model when a path may traverse a symlink
  outside the working directory.
ccVersion: 2.1.206
-->
Path contains '..' traversal after a directory segment, which may follow a symlink outside the working directory
