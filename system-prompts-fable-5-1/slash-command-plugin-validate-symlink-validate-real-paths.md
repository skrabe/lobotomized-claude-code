<!--
name: 'Slash Command: Plugin validate — validate symlink targets separately'
description: >-
  Tells the model that validation does follow symlinks here, so the real paths
  behind them have to be validated on their own.
ccVersion: 2.1.233
-->
 does follow them, so validate the real paths separately.
