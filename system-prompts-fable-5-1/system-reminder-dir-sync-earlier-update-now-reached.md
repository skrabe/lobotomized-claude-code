<!--
name: Dir Sync Earlier Update Now Reached
description: >-
  Model reminder that a previously not-taken directory-sync update has now
  reached the sync service so the user's machine receives it as usual.
ccVersion: 2.1.265
-->
Directory sync: the earlier update that the sync service had not taken has now reached it; the user's machine receives it as usual.
