<!--
name: Teammate prompt protocol-frame rejection
description: >-
  Error tool-result thrown from the Teammate/subagent launch when the model's
  prompt input is a mailbox protocol frame (permission/mode/plan/shutdown JSON);
  surfaced to the model to make it pass plain-text instructions.
ccVersion: 2.1.206
-->
Teammate prompt must not be a mailbox protocol frame (permission/mode/plan/shutdown JSON) — pass plain-text instructions
