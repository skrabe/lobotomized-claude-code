<!--
name: 'Tool Result: Artifact Publish Host Grant Unreadable MCP Rejected'
description: >-
  Lead of the publish tool_result when the local-server grant cannot be read, so
  the MCP manifest is rejected.
ccVersion: 2.1.273
variables:
  - TOOL_RESULT_ARTIFACT_PUBLISH_HOST_GRANT_UNREADABLE_MCP_REJECTED_VAR_0
-->
mcp manifest rejected: this computer's local-server grant (${TOOL_RESULT_ARTIFACT_PUBLISH_HOST_GRANT_UNREADABLE_MCP_REJECTED_VAR_0}, set by the app that launched this session; not something to fix from here) could not be read, so 
