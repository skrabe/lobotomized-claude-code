<!--
name: 'Data: extraKnownMarketplaces.source.(npm).version setting description'
description: >-
  Description of the `extraKnownMarketplaces.source.(npm).version` setting in
  Claude Code's settings JSON schema. The model reads it through /update-config
  and settings validation errors; it is also shown to users in the settings
  help.
ccVersion: 2.1.276
-->
Version or range to fetch (e.g. "1.4.0", "^1.4"); defaults to the latest dist-tag
