<!--
name: ExitPlanMode allowedPrompts tool parameter
description: >-
  input_schema param description for the ExitPlanMode tool's
  allowedPrompts[].tool field; serialized into the model's tool list, so
  model-facing.
ccVersion: 2.1.191
-->
The tool this prompt applies to
