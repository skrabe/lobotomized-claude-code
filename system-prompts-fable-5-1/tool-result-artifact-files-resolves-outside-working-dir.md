<!--
name: 'Tool result: Artifact file resolves outside working directory (cont.)'
description: >-
  Continuation fragment of the error returned to the model when a `files` source
  resolves outside the working directory (via a symlink).
ccVersion: 2.1.218
-->
directory (symlink?) — only files under it can be published
