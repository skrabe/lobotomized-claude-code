<!--
name: 'Tool Result: Worktree git guard — env chdir to runtime path'
description: >-
  Refusal clause: env changes directory to a runtime-computed location before
  running git.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_ENV_CHDIR_RUNTIME_VAR_0
-->
changes directory via env to a location computed at runtime (${TOOL_RESULT_BASH_WORKTREE_GIT_ENV_CHDIR_RUNTIME_VAR_0}) before running git
