<!--
name: 'Tool parameter: Artifact action'
description: >-
  Input-schema description for the Artifact tool's action enum (publish/list)
  shown to the model.
ccVersion: 2.1.210
-->
Omit (or 'publish') to publish file_path. 'list' enumerates artifacts — the user's own by default, see `scope`; only `limit` and `scope` may accompany it.
