<!--
name: 'Data: statusLine.(command).hideVimModeIndicator setting description'
description: >-
  Description of the `statusLine.(command).hideVimModeIndicator` setting in
  Claude Code's settings JSON schema. The model reads it through /update-config
  and settings validation errors; it is also shown to users in the settings
  help.
ccVersion: 2.1.276
-->
Hide the built-in `-- INSERT --` / `-- VISUAL --` indicator below the prompt. Use this when your status line script renders `vim.mode` itself.
