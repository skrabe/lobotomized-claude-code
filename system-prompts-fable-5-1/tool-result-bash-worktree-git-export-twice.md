<!--
name: 'Tool Result: Worktree git guard — variable exported more than once'
description: >-
  Refusal clause: a var is exported more than once (a += append or
  double-export), which can't be verified.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_EXPORT_TWICE_VAR_0
-->
exports ${TOOL_RESULT_BASH_WORKTREE_GIT_EXPORT_TWICE_VAR_0} more than once across the command, which cannot be verified (a += append or double-export)
