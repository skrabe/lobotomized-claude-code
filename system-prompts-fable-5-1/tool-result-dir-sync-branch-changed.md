<!--
name: 'Tool Result: Dir Sync Branch Changed'
description: >-
  Cause when the user switched the host checkout to another branch, so local
  edits are held back.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_DIR_SYNC_BRANCH_CHANGED_VAR_0
-->
the user has switched the checkout on ${TOOL_RESULT_DIR_SYNC_BRANCH_CHANGED_VAR_0} to another branch than the one this session's changes were made on, so your edits here are held back from it
