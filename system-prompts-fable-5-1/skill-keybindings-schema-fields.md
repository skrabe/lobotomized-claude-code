<!--
name: Keybindings skill schema fields rule
description: >-
  Keybindings skill fragment telling the model to always include the $schema and
  $docs fields.
ccVersion: 2.1.206
-->
Always include the `$schema` and `$docs` fields.
