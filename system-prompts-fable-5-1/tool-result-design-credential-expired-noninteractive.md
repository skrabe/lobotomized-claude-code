<!--
name: ClaudeDesign Credential Expired (Non-Interactive)
description: >-
  ClaudeDesign auth-error message built by mDy() and thrown as a Bn tool error;
  surfaced to the model as the ClaudeDesign tool_result when the credential is
  expired in a non-interactive session.
ccVersion: 2.1.207
variables:
  - TOOL_RESULT_DESIGN_CREDENTIAL_EXPIRED_NONINTERACTIVE_VAR_0
-->
${TOOL_RESULT_DESIGN_CREDENTIAL_EXPIRED_NONINTERACTIVE_VAR_0} The claude.ai credential is expired or revoked, and /login requires an interactive terminal — re-authenticate outside this session.
