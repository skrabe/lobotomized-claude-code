<!--
name: Tool param constraint (allOf)
description: >-
  Input-constraint note prepended to a tool description when a normalized
  JSON-Schema allOf means all listed parameters apply together; sent to the
  model as part of the tool description.
ccVersion: 2.1.206
-->
Input constraint: all listed parameters apply together (flattened from a JSON Schema allOf).
