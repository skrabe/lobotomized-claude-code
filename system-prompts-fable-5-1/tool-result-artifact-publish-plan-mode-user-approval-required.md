<!--
name: Artifact publish from plan mode — user approval required
description: >-
  safetyCheck decisionReason on the publish ask when publishing from plan mode
  moves plan content to a web-reachable page.
ccVersion: 2.1.239
variables:
  - TOOL_RESULT_ARTIFACT_PUBLISH_PLAN_MODE_USER_APPROVAL_REQUIRED_VAR_0
-->
Publishing from plan mode moves plan content to a web-reachable page — approval must come from the user, not the auto-permission classifier${TOOL_RESULT_ARTIFACT_PUBLISH_PLAN_MODE_USER_APPROVAL_REQUIRED_VAR_0}
