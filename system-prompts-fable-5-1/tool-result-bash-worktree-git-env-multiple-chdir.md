<!--
name: 'Tool Result: Worktree git guard — multiple env -C/--chdir'
description: >-
  Refusal clause: env is given more than one -C/--chdir (last-wins), which can't
  be verified.
ccVersion: 2.1.218
-->
passes more than one -C/--chdir to env (last-wins semantics), which cannot be verified
