<!--
name: 'Tool Result: Allowed Domains Unsandboxed'
description: >-
  SandboxPolicyRefusalError when a command approved with allowed_domains would
  run outside the sandbox, so the lists cannot be enforced and the command is
  not run.
ccVersion: 2.1.268
-->
This command was approved with allowed_domains but would now run outside the sandbox, which cannot enforce them; it was not run. Re-run it.
