<!--
name: 'Tool Result: Dir Sync Unreachable Resync Remedy'
description: >-
  Remedy on the unreachable pre-forward hold: the next user message resyncs, and
  an unreachable machine would not have received the command either.
ccVersion: 2.1.251
-->
the user's next message resyncs; if the machine is unreachable the command would not have reached it either
