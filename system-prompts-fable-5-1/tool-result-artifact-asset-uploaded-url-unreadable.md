<!--
name: Artifact Asset Uploaded URL Unreadable
description: >-
  upload_asset tool_result clause when the record's copy of the stored asset url
  is unreadable.
ccVersion: 2.1.265
variables:
  - TOOL_RESULT_ARTIFACT_ASSET_UPLOADED_URL_UNREADABLE_VAR_0
-->
is stored in the artifact, but this record's copy of its url is unreadable — ${TOOL_RESULT_ARTIFACT_ASSET_UPLOADED_URL_UNREADABLE_VAR_0('action "list_assets"',()=>'action "list" with `scope: "assets"`')} shows the real one; reference nothing from this record.
