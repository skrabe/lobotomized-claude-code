<!--
name: 'Tool Parameter: Artifact Lang'
description: >-
  Claude-voice lang .describe() on the Artifact input schema: html lang from the
  page's language, passed on every publish.
ccVersion: 2.1.269
-->
The standard language tag of the page's text ("ja", "pt-BR"), which becomes the page's <html lang>. Claude matches the content's main language, not the conversation's, and passes it on every publish of a page.
