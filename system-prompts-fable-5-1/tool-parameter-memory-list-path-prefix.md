<!--
name: 'Tool Parameter: memory_list path_prefix'
description: >-
  The path_prefix parameter description on the memory_list tool input schema,
  explaining directory-aligned prefix filtering.
ccVersion: 2.1.246
-->
Optional directory prefix to list only documents under it (e.g. /feedback/). Matching is directory-aligned (/x is the same as /x/). Omit to list the whole store.
