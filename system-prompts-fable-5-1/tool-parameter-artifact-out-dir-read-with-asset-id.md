<!--
name: 'Tool Parameter: Artifact out_dir Read With Asset Id'
description: >-
  out_dir schema description for reading an uploaded asset by path id into a
  local file.
ccVersion: 2.1.269
-->
read with an asset id as `path`: the directory to save into (default: the working directory). The file is named by the id plus its type's extension.
