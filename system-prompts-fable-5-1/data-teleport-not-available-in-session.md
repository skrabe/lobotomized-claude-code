<!--
name: 'Data: /teleport unavailable in this session'
description: 'Explanatory stdout returned for /teleport on a headless/cloud surface; wrapped in <local-command-stdout> and pushed into the conversation, so the model reads it and relays the instruction.'
ccVersion: 2.1.224
-->
