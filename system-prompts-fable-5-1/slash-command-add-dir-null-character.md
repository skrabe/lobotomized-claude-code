<!--
name: 'Slash Command: Add dir null character'
description: >-
  Local-jsx /add-dir onDone message when the path contains a null character and
  cannot be used as a working directory.
ccVersion: 2.1.251
variables:
  - SLASH_COMMAND_ADD_DIR_NULL_CHARACTER_VAR_0
-->
Path ${SLASH_COMMAND_ADD_DIR_NULL_CHARACTER_VAR_0} contains a null character, so it can't be used as a working directory. Remove the null character from the path or from the settings entry that lists it.
