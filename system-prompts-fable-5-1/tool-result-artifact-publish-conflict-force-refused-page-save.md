<!--
name: Artifact publish force refused over page save
description: >-
  Tail appended when force was refused: the server rejects force:true over a
  version saved from inside the page and accepts only a publish built on that
  version.
ccVersion: 2.1.239
-->
 The server refuses force:true over a version saved from inside the page; only a publish built on that version is accepted.
