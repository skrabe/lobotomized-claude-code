<!--
name: 'Slash Command: Output Style Local Settings Unavailable'
description: >-
  /output-style text telling the model the style cannot be changed because local
  settings are not loaded.
ccVersion: 2.1.269
-->
Output styles are saved to local settings (.claude/settings.local.json), which this session doesn't load, so the style can't be changed here.
