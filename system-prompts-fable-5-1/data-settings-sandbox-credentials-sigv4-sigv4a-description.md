<!--
name: 'Data: sandbox.credentials.sigv4.sigv4a setting description'
description: >-
  Description of the `sandbox.credentials.sigv4.sigv4a` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Policy for SigV4A (AWS4-ECDSA-P256-SHA256) asymmetric signatures: there is no shared-key HMAC to recompute. `deny` (default) or `passthrough`.
