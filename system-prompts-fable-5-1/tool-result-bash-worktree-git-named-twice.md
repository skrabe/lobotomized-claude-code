<!--
name: 'Tool Result: Worktree git guard — git named more than once'
description: >-
  Refusal clause: git is named more than once in one command, which can't be
  verified to stay inside the worktree.
ccVersion: 2.1.238
-->
names git more than once in a single command, which cannot be verified to stay inside the worktree
