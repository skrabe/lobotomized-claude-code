<!--
name: ExitPlanMode allowedPrompts action description
description: >-
  The `prompt` input-schema param description for the ExitPlanMode tool's
  allowedPrompts entries, serialized into the model's tool list.
ccVersion: 2.1.191
-->
Semantic description of the action, e.g. "run tests", "install dependencies"
