<!--
name: 'Tool Result: Dir Sync Upload Only'
description: >-
  go_with_note when the host only sends files here and cannot take this
  session's changes back.
ccVersion: 2.1.251
variables:
  - TOOL_RESULT_DIR_SYNC_UPLOAD_ONLY_VAR_0
-->
${TOOL_RESULT_DIR_SYNC_UPLOAD_ONLY_VAR_0} only sends files to this session and does not take this session's changes back (it is not bound to the session as a device, or cannot take files back); the command ran on the user's files as they are there
