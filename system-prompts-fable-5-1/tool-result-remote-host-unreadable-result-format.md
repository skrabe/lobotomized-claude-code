<!--
name: 'Tool Result: Remote Host Unreadable Result Format'
description: >-
  transport_error tool_result when the host answered in a result format this
  session cannot read, warning the command probably ran there.
ccVersion: 2.1.246
variables:
  - TOOL_RESULT_REMOTE_HOST_UNREADABLE_RESULT_FORMAT_VAR_0
-->
${TOOL_RESULT_REMOTE_HOST_UNREADABLE_RESULT_FORMAT_VAR_0} answered in a result format this session cannot read (Claude Code version mismatch?); the command probably ran there — check its effect before repeating it.
