<!--
name: 'Data: settings validation tip (apiKeyHelper wrong type)'
description: >-
  Suggestion appended to a settings.json validation error (apiKeyHelper wrong
  type); the model reads it in the tool result when its edit to a settings file
  fails validation.
ccVersion: 2.1.276
-->
Provide a shell command that outputs your API key to stdout. The script should output only the API key. Example: "/bin/generate_temp_api_key.sh"
