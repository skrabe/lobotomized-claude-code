<!--
name: 'Data: Agent Import — Hooks Unexpected Shape'
description: >-
  Unmappable `reason` when a foreign hooks block is not an event-keyed object;
  interpolated into the /import summary prompt sent to the model.
ccVersion: 2.1.265
-->
Has an unexpected shape (expected a `hooks` object keyed by event). Re-add any hooks you need under the `hooks` key in settings.json.
