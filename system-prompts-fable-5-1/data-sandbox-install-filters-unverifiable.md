<!--
name: 'Data: Sandbox install — filters unverifiable'
description: >-
  Status stem for /sandbox install on Windows when the WFP state is
  'cannot-read'; interpolated into the {status,message} result that onDone emits
  as <local-command-stdout> in a role:"user" message.
ccVersion: 2.1.214
-->
Sandbox user and network filters installed (filters can't be verified from a non-elevated process)
