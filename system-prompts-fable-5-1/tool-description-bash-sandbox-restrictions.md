<!--
name: Bash sandbox restrictions header
description: Header line of the command-sandbox section of the Bash tool description.
ccVersion: 2.1.206
-->

The sandbox has the following restrictions:

Provide one effective per-request sandbox snapshot containing the current mode, writable roots, network allowlist, socket policy, protected environment variables, and whether sandbox bypass is available.
