<!--
name: Host App Denied
description: >-
  Error tool result when an app_* call targets Claude's own host bundle id
  (would deadlock Accessibility).
ccVersion: 2.1.246
-->
Cannot target the host application itself — the app-scoped executor 
