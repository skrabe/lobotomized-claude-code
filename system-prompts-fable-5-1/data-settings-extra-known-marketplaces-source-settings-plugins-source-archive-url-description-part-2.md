<!--
name: >-
  Data: extraKnownMarketplaces.source.(settings).plugins.source.(archive).url
  setting description (part 2 of 2)
description: >-
  Description of the
  `extraKnownMarketplaces.source.(settings).plugins.source.(archive).url`
  setting in Claude Code's settings JSON schema. The model reads it through
  /update-config and settings validation errors; it is also shown to users in
  the settings help.
ccVersion: 2.1.276
-->
or nested one directory deep — a single wrapping directory is stripped.
