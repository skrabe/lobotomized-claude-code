<!--
name: 'Tool Parameter: AppifactRepl skill'
description: >-
  Names an optional loaded appifact skill whose scripts/appifact_sdk.js should
  run instead of the built-in SDK.
ccVersion: 2.1.273
-->
Leave it out: the built-in SDK runs the code. Only to run a loaded appifact skill's own scripts/appifact_sdk.js instead: that skill's name.
