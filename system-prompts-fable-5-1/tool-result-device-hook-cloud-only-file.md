<!--
name: 'Tool Result: Device Hook Cloud Only File'
description: >-
  PreToolUse deny reason when the event names a file that exists only in the
  cloud, so the attached machine could not judge it.
ccVersion: 2.1.246
-->
not run — the event names a file that exists only in the cloud, so this machine could not judge it
