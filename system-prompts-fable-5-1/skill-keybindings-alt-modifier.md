<!--
name: Keybindings skill alt modifier note
description: Keybindings skill fragment documenting the alt/opt/option modifier aliases.
ccVersion: 2.1.206
-->
- `alt` (aliases: `opt`, `option`) — note: `alt` and `meta` are identical in terminals
