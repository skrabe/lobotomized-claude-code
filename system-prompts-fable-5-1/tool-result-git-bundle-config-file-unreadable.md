<!--
name: 'Tool Result: Git Bundle Config File Unreadable'
description: >-
  pC clause when a named git config file could not be read as a plain file git
  would parse.
ccVersion: 2.1.280
variables:
  - TOOL_RESULT_GIT_BUNDLE_CONFIG_FILE_UNREADABLE_VAR_0
  - TOOL_RESULT_GIT_BUNDLE_CONFIG_FILE_UNREADABLE_VAR_1
-->
its git configuration file ${TOOL_RESULT_GIT_BUNDLE_CONFIG_FILE_UNREADABLE_VAR_0} could not be read as a plain file git would parse, so what that capture would run under cannot be judged here; ${TOOL_RESULT_GIT_BUNDLE_CONFIG_FILE_UNREADABLE_VAR_1}check that file, or start from an ordinary clone
