<!--
name: 'Tool Parameter: Artifact Paths Read File'
description: >-
  Person-facing Artifact read paths parameter: several published paths in place
  of path, saved like a single path with per-file results.
ccVersion: 2.1.273
variables:
  - TOOL_PARAMETER_ARTIFACT_PATHS_READ_FILE_VAR_0
-->
read: several published paths in place of \`path\`, up to ${TOOL_PARAMETER_ARTIFACT_PATHS_READ_FILE_VAR_0} in one call. Each file is saved as a single \`path\` would be, and the result lists where each one landed, or why it could not be read, with small text files' contents included while they fit.
