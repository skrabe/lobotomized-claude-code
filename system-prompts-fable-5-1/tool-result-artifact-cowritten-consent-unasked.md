<!--
name: Artifact Cowritten Consent Unasked
description: >-
  ArtifactInputError returned when a co-writer has published to the artifact so
  files/assets are withheld until the user is asked.
ccVersion: 2.1.261
variables:
  - TOOL_RESULT_ARTIFACT_COWRITTEN_CONSENT_UNASKED_VAR_0
-->
a co-writer has published to this artifact, so its ${TOOL_RESULT_ARTIFACT_COWRITTEN_CONSENT_UNASKED_VAR_0} are someone else's content — nothing was returned; retry the same action so it is checked again (the user is asked once where a prompt can reach them)
