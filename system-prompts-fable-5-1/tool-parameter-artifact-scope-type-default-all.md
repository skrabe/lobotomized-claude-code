<!--
name: 'Tool Parameter: Artifact Scope Type Default All'
description: >-
  scope .describe() addendum: with type or type_url the default is all (owned
  plus org-shared).
ccVersion: 2.1.257
-->
 With `type` or `type_url` (the Artifacts made from a type) the default is 'all': the user's own and the ones shared across their organization.
