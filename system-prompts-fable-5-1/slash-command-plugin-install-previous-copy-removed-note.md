<!--
name: 'Slash Command: /plugin Install Note — Previous Copy Had To Be Removed'
description: >-
  Sentence appended to the failed plugin-version install error when the
  previously installed copy had to be deleted first because the filesystem
  refused to rename it, so that version is no longer installed.
ccVersion: 2.1.277
-->
 The previously installed copy had to be removed first (this filesystem refuses to rename it), so this plugin version is not installed now.
