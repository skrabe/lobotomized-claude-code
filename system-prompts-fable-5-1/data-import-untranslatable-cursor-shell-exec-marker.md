<!--
name: 'Data: Import Untranslatable Cursor Shell-Exec Marker'
description: >-
  Unmappable-item reason when a Cursor command markdown contains a shell-exec
  marker that is inert in Cursor but live in Claude Code; interpolated into the
  import-to-claude-code SKILL.md.
ccVersion: 2.1.265
-->
Contains a `` !`…` `` or ```! shell-exec marker (inert in Cursor, live in Claude Code). Port it manually.
