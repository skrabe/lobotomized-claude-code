<!--
name: 'Tool Parameter: Artifact Files (List Form) Path'
description: >-
  List-form `files[].path` on the artifact publish tool: path relative to the
  working directory or `root` at which the file is served.
ccVersion: 2.1.265
-->
Path relative to the working directory (or to `root`, which may be a folder in your scratchpad directory); the file is served at this same path next to the page.
