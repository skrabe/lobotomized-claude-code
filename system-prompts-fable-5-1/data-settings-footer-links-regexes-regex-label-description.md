<!--
name: 'Data: footerLinksRegexes.(regex).label setting description'
description: >-
  Description of the `footerLinksRegexes.(regex).label` setting in Claude Code's
  settings JSON schema. The model reads it through /update-config and settings
  validation errors; it is also shown to users in the settings help.
ccVersion: 2.1.276
-->
Badge text. {name} placeholders filled from named capture groups; defaults to the full match.
