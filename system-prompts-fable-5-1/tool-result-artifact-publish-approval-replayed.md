<!--
name: Artifact Publish Approval Replayed
description: >-
  Publish tool error when this run reused an approval already consumed by an
  earlier run of the same tool call.
ccVersion: 2.1.267
-->
file_path: the approval for this publish was already used by an earlier run of the same tool call, so nothing was published by this run — retry the publish as a new call
