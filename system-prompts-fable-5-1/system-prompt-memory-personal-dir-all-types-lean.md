<!--
name: 'System Prompt: Personal directory holds all types (lean)'
description: >-
  Lean-variant memory-section line: with all shared stores read-only, save every
  memory type into the personal memory directory.
ccVersion: 2.1.224
variables:
  - SYSTEM_PROMPT_MEMORY_PERSONAL_DIR_ALL_TYPES_LEAN_VAR_0
-->
Save every memory type in your personal memory directory at \`${SYSTEM_PROMPT_MEMORY_PERSONAL_DIR_ALL_TYPES_LEAN_VAR_0}\` with the file tools; the shared stores are read-only this session. ${"Your personal memory directory already exists — write to it directly with the Write tool (do not run mkdir or check for its existence)."}
