<!--
name: /loop schedule autonomous-default prompt
description: >-
  Prompt returned by /loop to schedule the autonomous default and run the first
  check, sent to the model.
ccVersion: 2.1.206
variables:
  - SLASH_COMMAND_LOOP_SCHEDULE_AUTONOMOUS_VAR_0
-->
# /loop — schedule the autonomous default

The user invoked \`/loop\` with no prompt (input was empty or just the interval \`${SLASH_COMMAND_LOOP_SCHEDULE_AUTONOMOUS_VAR_0}\`). Schedule the autonomous-loop default and then run the first autonomous check immediately.
