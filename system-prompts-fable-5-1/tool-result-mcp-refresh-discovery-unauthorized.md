<!--
name: 'Tool Result: MCP Refresh Discovery Unauthorized'
description: >-
  Error string returned when an MCP server rejects tool discovery as
  unauthorized, telling the model the user must authorize the connector via
  /mcp; surfaced in the refresh-MCP-tools tool result.
ccVersion: 2.1.211
-->
the server rejected tool discovery as unauthorized — the user needs to authorize this connector (e.g. via /mcp) before its tools are available
