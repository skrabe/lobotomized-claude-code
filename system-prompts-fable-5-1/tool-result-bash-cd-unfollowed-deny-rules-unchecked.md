<!--
name: Bash Cd Unfollowed Deny Rules Unchecked
description: >-
  checkPermissions ask when directory changes before a command could not all be
  followed, so relative paths cannot be checked against Read/Edit deny rules.
ccVersion: 2.1.268
variables:
  - TOOL_RESULT_BASH_CD_UNFOLLOWED_DENY_RULES_UNCHECKED_VAR_0
  - TOOL_RESULT_BASH_CD_UNFOLLOWED_DENY_RULES_UNCHECKED_VAR_1
-->
the directory changes before '${TOOL_RESULT_BASH_CD_UNFOLLOWED_DENY_RULES_UNCHECKED_VAR_0(TOOL_RESULT_BASH_CD_UNFOLLOWED_DENY_RULES_UNCHECKED_VAR_1)}' could not all be followed, so the files its relative paths name cannot be checked against the configured Read/Edit deny rules
