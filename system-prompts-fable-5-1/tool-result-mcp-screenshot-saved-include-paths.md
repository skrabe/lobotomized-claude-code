<!--
name: 'Tool Result: Screenshot saved — include paths'
description: >-
  Note appended to an MCP screenshot tool result telling the model to include
  the saved screenshot path(s) in its response so they can be attached for the
  user.
ccVersion: 2.1.211
-->
Include the saved path(s) in your response so they can be attached for the user.
