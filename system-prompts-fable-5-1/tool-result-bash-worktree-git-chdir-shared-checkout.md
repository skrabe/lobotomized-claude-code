<!--
name: 'Tool Result: Worktree git guard — cd into shared checkout before git'
description: >-
  Refusal clause: the command changes directory into the shared checkout before
  running git.
ccVersion: 2.1.218
variables:
  - TOOL_RESULT_BASH_WORKTREE_GIT_CHDIR_SHARED_CHECKOUT_VAR_0
-->
changes directory to the shared checkout (${TOOL_RESULT_BASH_WORKTREE_GIT_CHDIR_SHARED_CHECKOUT_VAR_0}) before running git
