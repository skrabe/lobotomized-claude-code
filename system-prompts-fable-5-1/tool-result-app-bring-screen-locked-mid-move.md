<!--
name: 'Tool Result: App Bring Screen Locked Mid-Move'
description: >-
  Error returned to the model when the screen locks after
  app_bring_to_current_space has started, so the window was not moved.
ccVersion: 2.1.246
-->
The screen locked before the move ran, so the window was not moved. app_screenshot continues to work in background mode; try again once the user returns and the screen is unlocked.
