<!--
name: 'MCP reconnect: check-config hint'
description: >-
  Fragment of the /mcp slash-command result shown when a single server can't be
  reconnected; injected into the conversation as <local-command-stdout>.
ccVersion: 2.1.206
-->
Check its config with `/mcp` in the terminal.
