<!--
name: Appifact Upload Must Be In Folder
description: >-
  AppifactRepl invalid_args when upload_asset file_path is not a regular file
  the script wrote in its own publish folder.
ccVersion: 2.1.273
variables:
  - TOOL_RESULT_APPIFACT_UPLOAD_MUST_BE_IN_FOLDER_VAR_0
-->
file_path must be a regular file the script wrote inside its own publish folder (files.dir(); copy a file from elsewhere into it first), at most ${TOOL_RESULT_APPIFACT_UPLOAD_MUST_BE_IN_FOLDER_VAR_0>>20} MiB
