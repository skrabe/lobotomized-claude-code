<!--
name: 'Tool Description: Monitor Until Loop Instead Run In Background'
description: >-
  Monitor tool admonition not to use an unbounded command for a single
  notification; use Bash run_in_background with an until loop instead.
ccVersion: 2.1.268
-->
use Bash `run_in_background` with an `until` loop instead (one notification, ends in seconds)
