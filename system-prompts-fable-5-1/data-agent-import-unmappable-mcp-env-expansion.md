<!--
name: 'Data: Agent Import — MCP Env Expansion'
description: >-
  Refused-import `reason` when a repo MCP url/command/args references `${...}`
  that Claude Code would expand; interpolated into the /import summary prompt.
ccVersion: 2.1.265
-->
Its url, command or args reference an environment variable; on connect Claude Code would expand it into a repo-authored command or url. Add it manually with `claude mcp add` if you trust this repo.
