<!--
name: 'Tool Result: Git Bundle Env Worktree Config'
description: >-
  pC clause when launch-environment git config turns on
  extensions.worktreeConfig.
ccVersion: 2.1.280
variables:
  - TOOL_RESULT_GIT_BUNDLE_ENV_WORKTREE_CONFIG_VAR_0
-->
its launch environment’s git configuration switches per-worktree configuration on (extensions.worktreeConfig), under which that capture would also read a configuration file a session of the repository can plant in this tree’s administrative entry; ${TOOL_RESULT_GIT_BUNDLE_ENV_WORKTREE_CONFIG_VAR_0}start from the repository’s main checkout
