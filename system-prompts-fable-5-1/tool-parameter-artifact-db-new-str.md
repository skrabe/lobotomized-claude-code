<!--
name: 'Tool Parameter: Artifact DB new_str'
description: >-
  `new_str` on write_db str_replace: the replacement text, which may be empty to
  delete old_str.
ccVersion: 2.1.265
-->
write_db with db_op 'str_replace' only: the replacement text (may be empty to delete old_str).
