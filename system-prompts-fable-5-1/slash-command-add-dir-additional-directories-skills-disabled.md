<!--
name: 'Slash Command: /add-dir — additional-directory skills disabled'
description: >-
  Tells the model the path is inside cwd but skills, commands, and agents from
  additional directories are disabled, so nothing was loaded.
ccVersion: 2.1.257
variables:
  - SLASH_COMMAND_ADD_DIR_ADDITIONAL_DIRECTORIES_SKILLS_DISABLED_VAR_0
-->
${SLASH_COMMAND_ADD_DIR_ADDITIONAL_DIRECTORIES_SKILLS_DISABLED_VAR_0}; skills, commands, and agents from additional directories are disabled in this session, so nothing was loaded.
