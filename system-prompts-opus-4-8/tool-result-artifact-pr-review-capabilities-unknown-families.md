<!--
name: PR Review Capabilities Declaration Unknown Families
description: >-
  k9o() manifest-validation failure telling Claude the review page's
  capabilities declaration carries families other than `mcp` and `self`; thrown
  as `the pr_review capabilities manifest failed validation: …`.
ccVersion: 2.1.221
-->

the review page capabilities declaration carries unknown capability families — only `mcp` and `self` have review-page meaning, so nothing unexamined rides to the control plane
