<!--
name: 'Data: Agent Import — Gemini mcpServers Unexpected Shape'
description: >-
  User-scope unmappable reason for a malformed `mcpServers` block in
  ~/.gemini/settings.json; injected into the `/import` prompt and the fallback
  SKILL.md.
ccVersion: 2.1.214
-->
Has an unexpected shape in ~/.gemini/settings.json (expected an object of named entries).
