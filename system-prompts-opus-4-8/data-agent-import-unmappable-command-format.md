<!--
name: 'Data: Agent Import — Command Missing Prompt Field'
description: >-
  User-scope unmappable reason when a foreign command lacks the expected
  `prompt` string; injected into the `/import` prompt and SKILL.md.
ccVersion: 2.1.214
-->
Doesn't match the expected command format (needs a 'prompt' string).
